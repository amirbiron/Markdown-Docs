# ROADMAP — מפרויקט בדפדפן למערכת תיעוד

מסמך תכנון. הוא נכתב לפני הקוד, כדי שנסכים על המבנה לפני שמשקיעים בו.

---

## הבעיה

היום המסמכים נשמרים ב-`localStorage` של הדפדפן. זה עובד — הם שורדים רענון, סגירת דפדפן ואפילו דיפלוי חדש — אבל זה נשבר ברגע שעוברים למכשיר אחר. פתחת את האתר בטלפון? הספרייה ריקה. ניקית נתוני אתר? המסמכים נעלמו.

יש גם באג שקט: אם ה-`localStorage` מתמלא, `save()` בולע את השגיאה ואף אחד לא יודע שהמסמך לא נשמר.

המטרה כאן היא לא רק "לשמור בשרת". המטרה היא שיהיו **פרויקטים**, ולכל פרויקט מסמכי Markdown משלו — כלומר להוסיף רמת היררכיה שלא קיימת היום בכלל.

---

## החלטות שהתקבלו

| ההחלטה | מה זה אומר בפועל |
|---|---|
| **משתמש יחיד** | אין הרשמה ואין ניהול משתמשים. כניסה אחת עם סיסמה. טבלת `users` קיימת בכל זאת מיום ראשון, כדי שהוספת משתמשים בעתיד לא תדרוש מיגרציה. יצירת המשתמש — ראו "אתחול המשתמש היחיד" למטה. |
| **פרויקט יכול להיות ציבורי** | עמודת `visibility` עם `private` או `public`. פרויקט ציבורי נקרא בלי כניסה; כתיבה תמיד דורשת אימות. |
| **האחרון ששומר דורס** | אין נעילה ואין 409. שני טאבים על אותו מסמך — האחרון מנצח. "האחרון" מוגדר לפי מונה שהלקוח שולח, לא לפי סדר ההגעה — ראו "סדר הכתיבות" למטה. טבלת הגרסאות היא רשת הביטחון. |
| **Markdown ב-Postgres** | עמודת TEXT. לא קבצים ב-Git. |
| **מרחיבים את הפרונט הקיים** | `index.html` נשאר, שני העיצובים נשארים, `localStorage` מוחלף בקריאות API. |

### על מונגו — לא

השאלה הוגנת, והתשובה כאן ברורה יחסית. הנתונים כאן יחסיים לגמרי: משתמש מחזיק פרויקטים, פרויקט מחזיק מסמכים, מסמך מחזיק גרסאות. כל שאילתה מעניינת חוצה את הגבולות האלה.

שלושה דברים שאנחנו צריכים ושמונגו לא נותן בלי עבודה:

אילוץ `UNIQUE (project_id, slug)` הוא מה שפותר את ה-race condition ביצירת מסמך — בלי צורך ב-lock בקוד. חיפוש טקסט מלא בעברית עם דירוג הוא שורה אחת של `tsvector`. ומחיקה מדורגת של פרויקט על כל מסמכיו וגרסאותיו היא `ON DELETE CASCADE`, לא לולאה שעלולה לקרוס באמצע ולהשאיר יתומים.

מונגו היה מוסיף מסד נתונים שני לתחזק, בלי לפתור שום דבר שפוסטגרס לא פותר. **מסד נתונים אחד.**

---

## הסכימה

חמש טבלאות.

```
users          projects          documents         document_versions    project_links
─────          ────────          ─────────         ─────────────────    ─────────────
id             id                id                id                   id
email          owner_id ────┐    project_id ──┐    document_id ──┐       project_id ──┐
password_hash  slug         │    slug         │    content       │       title        │
session_version name        │    title        │    created_at    │       url          │
created_at     description  │    content      │                  │       position     │
               visibility   │    position     │                  │       created_at   │
               created_at   │    created_at   │                  │                    │
               updated_at   │    updated_at   │                  │                    │
                     └──────┘          └──────┘           └───────┘             └──────┘
```

### למה `slug` ולא רק `id`

כי הכתובת `/p/nimbus-docs/getting-started` קריאה, ואפשר לשלוח אותה למישהו. הכתובת `/p/7/42` היא לא.

לשתי רמות ה-`slug` יש שני אילוצים **שונים**, וקל להתבלבל ביניהם:

```sql
projects:   UNIQUE (slug)               -- גלובלי
documents:  UNIQUE (project_id, slug)   -- בתוך הפרויקט בלבד
```

**ל-`documents` הייחודיות היא בתוך הפרויקט**, כי אנחנו דווקא רוצים שלשני פרויקטים יהיה מסמך `installation` משלהם.

**ל-`projects` הייחודיות היא גלובלית**, וזו לא החלטה אסתטית אלא הכרח שנובע מה-API. הנתיב הוא `/api/projects/:slug` — אין בו שום רכיב שמזהה בעלים. אם שני פרויקטים היו יכולים לחלוק `slug`, לא היה לשרת דרך לדעת על מי מדובר, וה-`GET`, ה-`PATCH` וה-`DELETE` היו פועלים על פרויקט שרירותי.

האלטרנטיבה הייתה `UNIQUE (owner_id, slug)` ונתיב שכולל בעלים, בסגנון `/api/u/:user/projects/:slug`. דחיתי אותה משתי סיבות: יש משתמש אחד, ולפרויקט ציבורי מגיעה כתובת קצרה שאפשר לשלוח בלי שם משתמש בתוכה.

**בוחרים אחת מהשתיים, לא חצי מכל אחת.** ההחלטה כאן היא ייחודיות גלובלית ונתיב בלי בעלים.

אם אי פעם נעבור לרב-משתמשים, זה בדיוק המקום שיצטרך מיגרציה — והיא לא כואבת: מוסיפים את הבעלים לנתיב, ומחליפים את האילוץ ב-`UNIQUE (owner_id, slug)`.

### למה `position` ולא סתם סדר לפי תאריך

תיעוד הוא לא בלוג. הסדר של המסמכים בסיידבר הוא החלטה של הכותב — "התקנה" בא לפני "הגדרות מתקדמות" גם אם נכתב אחריו. עמודת `position` נותנת שליטה ידנית, וגרירה בממשק בהמשך.

### למה טבלת גרסאות

זו הטבלה שקיימת "כדי לא להצטער". היום היא נראית מיותרת. ברגע שתמחק בטעות פסקה שכתבת לפני חודש, היא תהיה הדבר החשוב ביותר במערכת.

היא גם מה שהופך את "האחרון ששומר דורס" להחלטה נוחה במקום מסוכנת. הדריסה לא מאבדת מידע — היא רק מזיזה אותו לטבלה אחרת.

השמירה פשוטה: לפני כל `UPDATE` על `documents.content`, דוחפים את התוכן **הישן** ל-`document_versions`. בלי diff, בלי חוכמות — עותק מלא. מסמך Markdown הוא כמה עשרות קילובייטים; מאה גרסאות שלו הן עדיין כלום מבחינת אחסון.

### קישורי פרויקט

טבלה נפרדת ולא מערך JSON בתוך `projects`, כי אנחנו רוצים לשנות סדר ולמחוק קישור בודד בלי לשכתב את כל הרשומה.

מלכודת אחת שקל לפספס: המשתמש נותן כתובת, והממשק מציג אותה כ-`<a href>`. הכתובת `javascript:alert(1)` היא href חוקי לגמרי מבחינת HTML. **הוולידציה חייבת לאשר רק `http` ו-`https`** — לא לחסום `javascript` ברשימה שחורה, כי `data:` ו-`vbscript:` יעקפו אותה. רשימה לבנה של שתי סכמות, וזהו.

---

## בסיס הנתונים — מה למדנו מהסקיל

`docs/israeli-postgres-toolkit/SKILL_HE.md` רלוונטי כאן, ובארבעה מקומות הוא משנה מה שתכננתי:

**דירוג לפי משקל.** במקום `to_tsvector` שטוח, נותנים לכותרת משקל גבוה מהתוכן. מסמך שהמילה מופיעה בכותרת שלו צריך לצוף מעל מסמך שהיא מוזכרת בו באמצע פסקה:

```sql
setweight(to_tsvector('simple', coalesce(title, '')), 'A') ||
setweight(to_tsvector('simple', coalesce(content, '')), 'B')
```

**ניקוד.** התוסף `unaccent` מסיר דיאקריטיקה לטינית בלבד. על ניקוד עברי הוא לא עושה כלום — `unaccent('שָׁלוֹם')` מחזיר `'שָׁלוֹם'`. אם מישהו יכתוב מנוקד ויחפש בלי ניקוד, לא יהיו תוצאות. הפתרון הוא פונקציית `IMMUTABLE` שמסירה את הטווח `U+0591–U+05C7` במפורש, ושימוש בה גם בעמודה המחושבת וגם בשאילתה.

**Collation.** אם נמיין כותרות פרויקטים בעברית לפי ICU, שים לב ש-collation לא דטרמיניסטי לא יכול לגבות `UNIQUE` או אינדקס `btree`. כלומר ה-`slug` נשאר בהשוואה רגילה, ורק המיון לתצוגה משתמש ב-ICU.

**אזור זמן וקידוד.** `Asia/Jerusalem` ו-`UTF8` נקבעים ביצירת בסיס הנתונים. תיקון מאוחר של אחד מהם דורש מיגרציית נתונים, ולכן זו בדיקה של שלב 1 ולא של שלב 5.

הסקיל המלא כבר בריפו, ו-`scripts/hebrew-search-setup.sql` שבתוכו הוא כמעט העתק-הדבק לשלב 4 — הוא כבר מכיל את `strip_nikud`, את `pg_trgm`, ואת ה-`search_vector` עם המשקלים. מה שנשאר הוא להתאים שמות עמודות.

השאר בסקיל — מע"מ, תעודת זהות, טלפונים, הלוח העברי, RLS רב-דיירים — לא נוגע לפרויקט הזה.

---

## ה-API

```
                                    [פ] = בלי session מחזיר פומביים בלבד
                                    [א] = דורש session תמיד

POST   /api/auth/login              כניסה, מחזיר cookie
POST   /api/auth/logout

GET    /api/projects            [פ] בלי session — רק visibility='public'
POST   /api/projects            [א]
GET    /api/projects/:slug      [פ] פרויקט + מסמכים + קישורים; פרטי בלי
                                    session מחזיר 404, לא 403
PATCH  /api/projects/:slug      [א]
DELETE /api/projects/:slug      [א]

GET    /api/projects/:slug/docs/:docSlug
POST   /api/projects/:slug/docs
PUT    /api/projects/:slug/docs/:docSlug
DELETE /api/projects/:slug/docs/:docSlug

GET    /api/projects/:slug/links
POST   /api/projects/:slug/links
PATCH  /api/projects/:slug/links/:id
DELETE /api/projects/:slug/links/:id

GET    /api/search?q=...        [פ] בלי session מחפש בפומביים בלבד
GET    /api/backup.zip          [א] מייצא את כל הנתונים — לעולם לא פומבי
```

### שלוש החלטות שכדאי להסביר

**Cookie ולא JWT ב-localStorage.** טוקן ב-`localStorage` נגיש לכל JavaScript שרץ בדף, כולל סקריפט שהוזרק. Cookie עם `HttpOnly` לא נגיש ל-JS בכלל. הפירוט המלא של מה שנדרש כדי שזה באמת יחזיק נמצא בסעיף האימות למטה.

**`login` מוגן מפני ניחוש.** משתמש יחיד עם סיסמה אחת הוא יעד נוח: אין מה למנות, רק מה לנחש. לכן החסימה קודמת לאימות עצמו — סופרים ניסיונות כושלים לפי IP וגם לפי חשבון, ואחרי חמישה כושלים ברצף מתחילים backoff מעריכי עד תקרה של כמה דקות. הספירה מתאפסת רק בכניסה מוצלחת.

התשובה על כישלון היא תמיד אותה תשובה: 401 עם הודעה גנרית בעברית, בלי לרמוז אם המייל קיים, אם הסיסמה שגויה, או אם החשבון חסום כרגע. גם זמן התגובה צריך להיות דומה בכל המקרים — `bcrypt` מספק את זה כשמריצים אותו גם על מייל שלא קיים, מול hash דמה.

**המרת Markdown ל-HTML נשארת בדפדפן.** הפרסר כבר קיים ועובד, וזה גם מה שמאפשר לייב פריוויו בלי לדבר עם השרת בכל הקלדה. השרת מחזיר `content` גולמי, והלקוח מרנדר.

**גבול גודל על `content`.** מסמך Markdown סביר הוא עד כמה מאות קילובייטים. בלי גבול, בקשה אחת עם 50MB של טקסט תתפוס את השרת. המספר: 1MB, ומחזירים 413 מעליו.

הנקודה שקובעת אם הגבול באמת מגן היא **מתי הוא נאכף.** ולידציה על `len(content)` אחרי שהבקשה כבר נקראה ופוענחה כ-JSON היא מאוחרת מדי — באותו רגע ה-50MB כבר בזיכרון, וזה בדיוק מה שרצינו למנוע. האכיפה היא ב-middleware שרץ לפני קריאת הגוף: `Content-Length` מעל הגבול נדחה מיד ב-413, ובקשה בלי `Content-Length` (כלומר `chunked`) נספרת תוך כדי קריאה ונקטעת ברגע שעברה. הוולידציה ברמת המודל נשארת, אבל כרשת שנייה בלבד.

הספירה היא ב**בתים של UTF-8** ולא בתווים. תו עברי הוא שני בתים, ולכן `len()` בפייתון על מחרוזת מחזיר בערך חצי מהגודל האמיתי של מסמך בעברית — גבול שנאכף על תווים הוא כפול ממה שהתכוונו אליו.

---

## העורך

זה החלק שהמשתמש יבלה בו הכי הרבה זמן, אז הוא ראוי לתכנון ולא רק לשורה ברשימה.

### לייב פריוויו

מסך מפוצל: מקור בצד אחד, תוצאה מרונדרת בצד השני. הפרסר כבר קיים ורץ בדפדפן, אז אין כאן קריאת רשת בכלל.

הנקודה שקובעת אם זה ירגיש טוב או גרוע היא **מתי מרנדרים מחדש.** רינדור בכל הקלדה נראה מפתה, אבל על מסמך של אלפי שורות הפרסר רץ מחדש על הכול בכל תו, וההקלדה מתחילה לקרטע. הפתרון הוא debounce קצר — בסביבות 150 מילישניות. מספיק קצר כדי שירגיש מיידי, מספיק ארוך כדי שהקלדה רציפה לא תפעיל אותו בכלל.

שני דברים נוספים שנדרשים כדי שזה יהיה שמיש:

סנכרון גלילה בין החלוניות. כשגוללים במקור, התצוגה זזה למקום המקביל. המימוש הפשוט הוא לפי אחוזים; המימוש הנכון הוא לפי בלוקים, כי בלוק קוד ארוך מזיז את היחס. נתחיל באחוזים.

שמירה אוטומטית, גם היא עם debounce — אבל ארוך יותר, כמה שניות. בלי זה, כל הקלדה היא `PUT` לשרת. עם אינדיקטור "נשמר" שקט, כדי שלא תצטרך לנחש.

הטעות הנפוצה כאן: לחשוב שהפריוויו הוא הבעיה. הסימפטום שמזהים אותה דרכו — העורך מרגיש חלק, אבל השרת מקבל מאתיים בקשות בדקה. הפתרון: שני ה-debounce נפרדים, וזה שמדבר עם השרת ארוך בהרבה.

### סדר הכתיבות

השמירה האוטומטית שולחת `PUT` כל כמה שניות, וכאן מתחבא באג שקשה מאוד לשחזר: **סדר ההגעה לשרת אינו סדר השליחה.** אם השמירה של שנייה 10 נתקעת ברשת והשמירה של שנייה 13 עוקפת אותה, השרת יכתוב קודם את החדשה ואז את הישנה. התוצאה היא מסמך שחוזר אחורה בזמן בלי שאף אחד נגע בו, וזה נראה כמו רוח רפאים.

"האחרון ששומר דורס" זו החלטה על **מי מנצח בין שני עורכים**, לא היתר לאבד כתיבות של אותו עורך.

הפתרון הוא מונה מונוטוני שהעורך מנהל בזיכרון ומעלה בכל שמירה:

```
PUT /api/projects/:slug/docs/:docSlug
{ "content": "...", "client_seq": 42 }
```

השרת שומר את ה-`client_seq` האחרון שקיבל לכל מסמך, וכותב רק אם המספר החדש גדול ממנו. בקשה ישנה שהגיעה מאוחר נדחית בשקט עם 200 — היא לא שגיאה, היא פשוט כבר לא רלוונטית, ואין סיבה להפחיד את המשתמש.

המונה מתאפס בטעינת המסמך, ולכן הוא לא צריך להישמר בין sessions — **אבל אז הוא חייב להיות משויך לעורך ולא למסמך.** זו נקודה שקל מאוד לפספס, והיא הופכת את המנגנון מהגנה לתקלה.

נניח מונה גלובלי אחד למסמך. טאב א' עורך זמן מה ומגיע ל-50. עכשיו נפתח טאב ב', או שאותו טאב נטען מחדש, והמונה מתחיל מ-1. כל כתיבה של ב' נמוכה מ-50, ולכן **אף אחת מהן לא תתקבל לעולם** — והמשתמש רואה עורך שפשוט מפסיק לשמור, בלי הודעת שגיאה ובלי סיבה נראית לעין.

לכן הבקשה נושאת גם `editor_id`, מזהה אקראי שכל עורך מייצר לעצמו:

```text
PUT /api/projects/:slug/docs/:docSlug
{ "content": "...", "client_seq": 42, "editor_id": "a3f9c1" }
```

השרת שומר את שניהם, ודוחה רק כשה-`editor_id` תואם וגם המונה לא עלה. בין עורכים שונים אין השוואה בכלל, ושם חל הכלל המוצהר: האחרון ששומר מנצח, עם טבלת הגרסאות מאחור.

### לחצני העזר

סרגל מעל תיבת הטקסט שמזריק תבניות Markdown. הקבוצות:

**עיצוב טקסט** — קישור, מודגש, נטוי, קו חוצה, הדגשה (`==`), קוד inline.
**מבנה** — כותרות, רשימה, רשימה ממוספרת, טבלה, קו מפריד, בלוק קוד.
**משימות** — רשימת צ'קבוקסים.
**בלוקים מתקפלים** — `::: details`.
**התראות** — כל הסוגים שהפרסר כבר תומך בהם: `note`, `tip`, `warning`, `danger`, `important`, `info`, `success`, `question`, `example`, `quote`, `experimental`, `deprecated`, `todo`, `abstract`.

הנקודה הטכנית היחידה שמשנה כאן, וקל לפספס אותה: **מה קורה כשיש טקסט מסומן.** אם המשתמש סימן "טקסט חשוב" ולחץ על כפתור ההדגשה, התוצאה צריכה להיות `==טקסט חשוב==` — לא `====` שדורס את מה שסימן. כלומר כל כפתור מוגדר כזוג `before`/`after` שעוטף את הסימון, ולא כמחרוזת אחת שמוחדרת.

אחרי ההזרקה הסמן חוזר לתוך התבנית, לא אחריה. מי שלוחץ על "קישור" רוצה להתחיל להקליד את הטקסט מיד.

הרשימה המלאה של התבניות תשב בקובץ נתונים אחד, לא מפוזרת בקוד — כדי שהוספת סוג התראה חדש תהיה שורה אחת.

**מה שהתברר במימוש:** ההתראות אינן נכתבות ברשימה נפרדת בכלל, אלא נגזרות מ-`CALLOUT` — הטבלה שהפרסר עצמו משתמש בה. שתי רשימות היו מתפצלות בשקט ברגע שמוסיפים סוג לאחת מהן. ובתצוגה הן רשימה נפתחת ולא 19 כפתורים: גליף בן תו אחד לכל סוג הוא בלתי קריא, והשמות המלאים נכנסים רק בתפריט.

### בלוקי קוד בעברית

בלוק קוד מוצג היום `dir="ltr"` תמיד, וזה נכון לקוד. אבל כשמשתמשים בגדר משולשת כדי לשים בתוכה טקסט עברי, התוצאה נשברת: הפיסוק קופץ לצד הלא נכון והשורות מיושרות הפוך.

המימוש מהפרויקט האחר מתועד ב-`docs/Markdown_RTL_Code_Block_GUIDE.md`, ויש בו תנאי אחד שקל מאוד לפספס.

**התנאי הראשון הוא לא כמות העברית — הוא האם הוגדרה שפה.** בלוק שנפתח ב-` ```js ` נשאר LTR גם אם כל ההערות בתוכו בעברית, כי זה קוד. רק בלוק בלי שפה — או עם `plaintext`, `text`, `nohighlight`, `none`, `txt` — הוא בכלל מועמד ל-RTL. בלי התנאי הזה, פונקציית JavaScript עם הערות עבריות הייתה מתהפכת, וזה הרבה יותר גרוע מהבעיה שניסינו לפתור.

רק אחריו נבדק היחס: יותר מ-30% מהתווים האלפביתיים בעברית. הספירה מתוך אותיות עבריות (`U+0590–U+05FF`) ואותיות לטיניות בלבד — ספרות, סימני פיסוק, רווחים וחיצים לא נספרים, אחרת בלוק עם הרבה סוגריים מדלל את היחס ומזייף את התוצאה.

**מה שונה אצלנו, לטובה.** במימוש המקורי הבדיקה רצה על ה-DOM אחרי הרינדור, ולכן היא חייבת לרוץ *לפני* מנוע הצביעה — אחרת המנוע מוסיף `language-*` משלו והבדיקה חושבת שהוגדרה שפה. אצלנו אין את הבעיה הזאת בכלל: הפרסר כבר מחזיק את `b.lang` מהגדר עצמו, בזמן הפרסור, לפני שראינו DOM. הבדיקה הופכת לתנאי על אובייקט הבלוק, בלי `closest('pre')` ובלי תלות בסדר קריאות.

באותו אופן, הקוד המקורי משכפל את ה-CSS בשלושה הקשרי תצוגה (דף MD, דפדפן הריפו, תצוגה מפוצלת). אצלנו יש מסלול רינדור אחד ש-`render_blocks` מייצר, והוא משרת גם את הצפייה וגם את הלייב פריוויו. אותה בדיקה, מקום אחד.

התוצאה בפועל: `dir="rtl"` ו-`textAlign: "right"` על ה-`<pre>`, במקום ה-`ltr` הקבוע שיש היום.

---

## גיבויים

שתי דרכים, ורק אחת מהן מסובכת.

**הורדה לפי דרישה** היא הפשוטה: `GET /api/backup.zip` בונה ZIP בזרימה ומחזיר אותו. בפנים — תיקייה לכל פרויקט, קובץ `.md` לכל מסמך, ו-`links.json` לקישורים. שום דבר לא נשמר בשרת, ולכן אין מה שיתקלקל.

יש בזה מלכודת אחת, והיא לא נראית עד שמסתכלים על הזמן. בנייה בזרימה מבצעת שאילתה לכל פרויקט בתורו, וכל שאילתה רואה את המצב **ברגע שלה**. אם עורכים מסמך באמצע הייצוא, הגיבוי יכול להכיל פרויקט אחד מלפני העריכה ופרויקט אחר מאחריה — או מסמך בגרסה חדשה שהגרסה הקודמת שלו כבר נדחפה לארכיון. גיבוי לא עקבי הוא גרוע מגיבוי חסר, כי הוא נראה תקין.

לכן כל הייצוא רץ בתוך טרנזקציה אחת ב-`REPEATABLE READ`, שנפתחת לפני הבייט הראשון ונסגרת אחרי האחרון. כל השאילתות רואות את אותו snapshot, והזרימה נשמרת — אין צורך להחזיק את הארכיון בזיכרון או על הדיסק.

**גיבוי מתוזמן** נכתב לדיסק קבוע ב-Render, שמחובר לשירות ושורד דיפלויים. זו הדרך הפשוטה, בהינתן שהדיסק אפשרי.

**המתזמן רץ בתוך שירות ה-web ולא כ-Cron Job**, וזו לא בחירה אלא אילוץ. התיעוד של Render מפורש: *"Cron jobs can't provision or access a persistent disk"*, ובצד השני *"A persistent disk is accessible by only a single service instance... You can't access a service's disk from any other service"*. שירות cron נפרד לא יכול לכתוב לדיסק הזה בכלל.

התזמון לא נספר מרגע העלייה אלא נשען על זמן הגיבוי האחרון שנמצא בתיקייה. בלי זה, דיפלוי באמצע היום היה מאפס את השעון — ואצל מי שמדפלוי לעיתים קרובות הגיבוי פשוט לא היה רץ אף פעם.

שלוש השלכות של חיבור דיסק, שכולן בסדר בפרויקט הזה אבל כדאי שתדע עליהן מראש: השירות מוגבל ל**מופע אחד** ולא יכול להתרחב לרוחב, הדיפלויים מפסיקים להיות zero-downtime כי Render חייב לעצור את המופע הישן לפני שהוא מפעיל את החדש, והדיסק דורש instance בתשלום. לאפליקציית תיעוד של משתמש יחיד — שלושתן לא רלוונטיות.

מה שכן צריך תשומת לב הוא **ניקוי**. גיבוי יומי שנשמר לנצח ימלא את הדיסק, ואז הכתיבה תיכשל — וכמו תמיד, בשקט. שמירה של שלושים גיבויים אחרונים ומחיקת הישנים היא חלק מהמשימה, לא שיפור עתידי.

הדיסק נותן עמידות מול דיפלוי, לא מול אובדן. אם השירות עצמו נמחק, הדיסק הולך איתו. לכן הגיבוי השבועי יוצא גם החוצה — קובץ לטלגרם, וכבר יש בוט.

**אבל לא ZIP גולמי.** הארכיון הזה הוא כל התוכן של המערכת, וטלגרם הוא צד שלישי שמחזיק אותו בשרתים שלו ללא הגבלת זמן. מי שמשיג גישה לחשבון — או לבוט — מקבל את כל המסמכים, כולל אלה שבפרויקטים הפרטיים. ההיסטוריה בצ'אט נשארת גם אחרי שהגיבוי כבר לא רלוונטי.

לכן הארכיון מוצפן לפני השליחה. המפתח **לא** יושב באותו מקום כמו הגיבוי: הוא במשתנה סביבה ב-Render ובעותק אחד מחוץ למערכת. גיבוי מוצפן שהמפתח שלו נשלח באותו ערוץ הוא גיבוי לא מוצפן.

**ההצפנה בתהליך ולא ב-`age`/`gpg`.** שניהם טובים, ושניהם בינאריים חיצוניים שאינם מובטחים ב-image של Render. כישלון שם היה שקט בדיוק כמו כל שאר הכשלים שהמסמך הזה מנסה למנוע: הגיבוי השבועי פשוט לא יוצא, ואף אחד לא מגלה עד הרגע שבו צריך אותו. במקום זה AES-256-GCM עם מפתח שנגזר ב-scrypt, ב-`cryptography`. GCM ולא CBC כי הוא מאמת — קובץ שהשתנה בדרך נכשל בפענוח במקום להתפענח לזבל.

בצד היעד: צ'אט ייעודי ולא קבוצה משותפת, ומחיקה של הודעות ישנות באותו קצב שבו נמחקים הגיבויים מהדיסק — אין סיבה שטלגרם יחזיק שנה אחורה כשהדיסק מחזיק שלושים יום.

מה שנשמר: הסכימה המלאה, כל המסמכים, הגרסאות והקישורים. מה שלא נשמר: ה-`password_hash`.

---

## אימות — מה ה-cookie באמת נותן

`HttpOnly` נשמע כמו התשובה, והוא לא. שווה לדייק מה הוא כן פותר ומה לא, כי הפער הזה הוא מקור נפוץ לביטחון עצמי מוגזם.

### מה HttpOnly לא מונע

`HttpOnly` מונע **קריאה** של ה-cookie מ-JavaScript. הוא לא מונע **שימוש** בו. סקריפט שהוזרק לדף לא יכול לקרוא את הטוקן — אבל הוא כן יכול לקרוא:

```js
fetch('/api/projects/secret-project', { method: 'DELETE', credentials: 'same-origin' })
```

והדפדפן יצרף את ה-cookie בשמחה. התוקף לא ראה את הטוקן, אבל הוא פעל בשמך.

המסקנה המעשית: `HttpOnly` מוריד את הנזק מ"גניבת זהות קבועה" ל"פעולות כל עוד הדף פתוח" — וזה שיפור אמיתי, אבל הוא לא מחליף את ההגנה מפני XSS מלכתחילה. **הגנת ה-XSS היא העיקר; ה-cookie הוא הפחתת נזק.**

### הדגלים

| דגל | ערך | למה |
|---|---|---|
| `HttpOnly` | `true` | חוסם קריאה מ-JS |
| `Secure` | `true` | ה-cookie לא נשלח על HTTP. ב-Render יש HTTPS ממילא, אבל בלי הדגל מספיק downgrade אחד |
| `SameSite` | `Lax` | לא נשלח בבקשות `POST`/`PUT`/`DELETE` שמקורן באתר אחר |
| `Path` | `/` | |
| `Max-Age` | 30 יום | |

### CSRF

`SameSite=Lax` כבר חוסם את התקיפה הקלאסית: טופס באתר זר ששולח `POST` לשרת שלנו לא יקבל את ה-cookie. זה מכסה את רוב השטח, ולכן **טוקן CSRF נפרד הוא מיותר כאן**.

שני סייגים שכן צריך לכבד:

`Lax` **כן** שולח cookie בניווט עליון מסוג `GET`. לכן כל בקשת `GET` ב-API חייבת להישאר נטולת תופעות לוואי. זה נשמע מובן מאליו עד שמישהו מוסיף `GET /api/projects/:slug/delete` כי "ככה קל יותר לבדוק".

ובנוסף, בדיקת `Origin` על כל בקשה משנה (`POST`, `PUT`, `PATCH`, `DELETE`). היא מכסה גם דפדפנים ישנים שלא מיישמים `SameSite` וגם את המקרה של subdomain שנפרץ.

הכלל הוא **allowlist ולא blocklist**: אם `Origin` חסר או לא תואם לדומיין שלנו — 403. הניסוח המתבקש, "אם הכותרת קיימת ולא תואמת — 403", נשמע זהיר אבל הוא פרצה: הוא מרשה כל בקשה שלא נשלח בה `Origin`, וזה בדיוק מה שתוקף ישלח.

הדפדפן שולח `Origin` בכל `fetch` שמשנה מצב, ולכן הלקוח שלנו לא נפגע. מה שכן ייחסם הוא `curl` בלי הכותרת — וזה מקובל, כי אלה בדיקות ידניות ולא לקוח אמיתי. אם בהמשך נצטרך לקוח שלא שולח `Origin`, התוספת היא טוקן CSRF מפורש לאותו מסלול, לא ויתור על הבדיקה.

### איפה ה-session יושב

שתי גישות, וההבדל ביניהן הוא שאלה אחת: האם אפשר לבטל session לפני שפג?

**Cookie חתום וחסר מצב** הוא הפשוט — אין טבלה, השרת מאמת חתימה. החיסרון: אי אפשר לבטל. מי שהשיג את ה-cookie נשאר בפנים עד שהוא פג, ואין כפתור שמנתק אותו.

**טבלת sessions** נותנת ביטול מיידי, במחיר של שאילתה בכל בקשה.

לפרויקט הזה יש דרך אמצע שנותנת את שתי התכונות: עמודת `session_version` על `users`. ה-cookie החתום נושא את המספר, והשרת משווה. העלאת המספר באחת מבטלת כל cookie קיים — כלומר "התנתקות מכל המכשירים" בלי טבלה ובלי שאילתה נוספת, כי המשתמש נטען ממילא.

### התפוגה חייבת להיות בתוך החתימה

`Max-Age` הוא **הוראה לדפדפן**, לא אכיפה. הוא אומר לדפדפן מתי להפסיק לשלוח את ה-cookie — אבל התוקף לא משתמש בדפדפן. מי שהעתיק את ערך ה-cookie יכול לשלוח אותו ב-`curl` שנה אחר כך, והחתימה עדיין תקפה, והשרת יקבל אותו. `Max-Age` לא נותן שום הגנה מול לקוח שלא משתף פעולה.

לכן ה-payload החתום נושא חותמת `exp`, והשרת בודק אותה **בכל בקשה**, לפני שהוא בכלל טורח לטעון את המשתמש:

```
payload = { "uid": ..., "sv": session_version, "exp": <unix timestamp> }
```

שלושת התנאים נבדקים יחד, וכולם חובה: החתימה תקפה, `exp` בעתיד, ו-`sv` תואם ל-`users.session_version`. אחד מהם נכשל — 401.

`Max-Age` נשאר בתור ניקיון: הוא מונע מהדפדפן לשלוח cookie שכבר מת. הוא לא שכבת אבטחה, ואין לספור אותו כאחת.

חלוקת התפקידים בין השלושה: `exp` מטפל בשגרה של "הזמן עבר", `session_version` בשגרה של "רוצה לנתק עכשיו", והחלפת הסוד בחירום.

### אתחול המשתמש היחיד

אין הרשמה, אז צריך תשובה לשאלה איך המשתמש היחיד נוצר מלכתחילה. התשובה היא **seed idempotent** שרץ בעלייה, לא מסך ולא endpoint.

הוא קורא שני משתני סביבה, `ADMIN_EMAIL` ו-`ADMIN_PASSWORD`, ומתנהג כך: אם אין משתמש עם המייל הזה — יוצר אותו עם `bcrypt` hash. אם יש — לא נוגע בו. הרצה שנייה לא משנה כלום, ולכן אפשר להריץ אותו בכל דיפלוי בלי לחשוב.

ארבעה דברים שחייבים להיות נכונים סביבו:

הסיסמה מגיעה ממשתנה סביבה בלבד. לא ב-`render.yaml` כערך, לא במיגרציה, ולא כברירת מחדל בקוד — כי כל אחד משלושת אלה נכנס ל-git ונשאר שם לנצח בהיסטוריה.

`ADMIN_PASSWORD` **לא** נכתב ללוג, ולא נכתב ה-hash. גם לא בשגיאה, גם לא ב-`repr` של אובייקט המשתמש. השורה היחידה שיוצאת ללוג היא "נוצר משתמש ראשוני" או "משתמש ראשוני קיים".

אורך מינימלי נאכף בעלייה. סיסמה קצרה מ-12 תווים מפילה את השרת עם הודעה ברורה במקום ליצור חשבון חלש בשקט.

שינוי `ADMIN_PASSWORD` אחרי היצירה **לא** מחליף את הסיסמה — ה-seed לא נוגע במשתמש שכבר קיים. החלפת סיסמה נעשית במפורש, והיא מעלה את `session_version` כדי לנתק את כל המכשירים.

### סוד החתימה

הסוד יושב במשתנה סביבה, לא בקוד. החלפתו מבטלת את כל ה-sessions הקיימים — וזו התנהגות רצויה, לא תקלה: זה מנגנון החירום אם יש חשד לדליפה. `session_version` הוא הכלי לשגרה, החלפת הסוד היא הכלי לחירום.

### ספריות מ-CDN

זה הווקטור שקל לשכוח, כי הוא לא בקוד שלנו. סקריפט מ-CDN שהוחלף רץ באותו הקשר כמו האתר — ואז כל מה שכתוב למעלה על XSS חל.

המצב היום מעורב:

**React, ReactDOM ו-Babel כבר מוגנים.** ה-runtime טוען אותם עם `integrity` ו-`crossOrigin` (`assets/support.js:1144-1148`), כך שקובץ שהשתנה פשוט לא ירוץ. זה כבר קיים ולא צריך לגעת בו.

**Prism ו-mermaid ירדו לריפו.** בוצע. הם יושבים ב-`assets/vendor/`, ו-`scripts/vendor.sh` מוריד אותם מחדש בשדרוג גרסה.

**מה שהניע את זה:** ה-autoloader של Prism הוריד קובצי דקדוק בזמן ריצה, לפי השפה שנתקל בה. הכתובות נבנו דינמית, ולכן לא היה להן `integrity`. כלומר ה-`integrity` שהוספנו לשלוש תגיות ה-`<script>` הגן על טעינת הספריות — ולא על מסלול טעינת השפות, שהוא זה שרץ בפועל בכל פעם שנפתח מסמך עם בלוק קוד.

**מה שנעשה:** Prism נארז מראש עם השפות שבשימוש (`markup`, `clike`, `javascript`, `typescript`, `jsx`, `tsx`, `css`, `python`, `bash`, `sql`, `json`, `yaml`, `markdown`), ה-autoloader הוסר, ו-mermaid, React ו-ReactDOM ירדו גם הם. React נטען מקומית דרך `window.__resources`, שהיא נקודת ההרחבה שה-runtime מספק לכך.

שפה שאינה בחבילה לא שוברת כלום — `codeHtml` מחזיר `null` והרינדור נופל בחזרה על מדגיש הסינטקס הפשוט שכבר קיים בקוד.

**מה שזה קנה ב-CSP:** `script-src` כבר לא מכיל שום מקור חיצוני, ו-`connect-src` הוא `'self'` בלבד. ה-`unsafe-inline` וה-`unsafe-eval` עדיין שם ועדיין לא יעצרו הזרקה — אבל קוד שכן הצליח לרוץ לא יכול למשוך קוד נוסף מבחוץ ולא יכול להבריח מידע החוצה.

**מה שנשאר חיצוני:** גופני Google בלבד. הם לא מריצים קוד, ולכן הם לא ווקטור הזרקה — הם עניין של פרטיות וזמינות. `scripts/check-offline.js` חוסם כל בקשה חיצונית ומוודא שהדף עדיין עולה, שהצביעה רצה, שהתרשים מצויר ושהאינטראקטיביות עובדת.

ה-CSP שכבר קיים ב-`render.yaml` מגביל את המקורות שמהם מותר לטעון קוד, אבל הוא מכיל `'unsafe-inline'` ו-`'unsafe-eval'` — שניהם נדרשים בפועל, כי ה-runtime מריץ `new Function` וכל העיצוב הוא inline styles. **המשמעות: ה-CSP הזה לא יעצור XSS.** הוא מגביל לאן אפשר להבריח מידע ומאיפה אפשר למשוך קוד, וזה שווה משהו — אבל אין להתייחס אליו כאל שכבת הגנה מפני הזרקה.

---

## אבטחה — מה שקל לפספס

הכללים בקובץ `CLAUDE.md` נוגעים ישירות לכמה מהנקודות כאן, אז אלה לא הערות תאורטיות.

**Race condition ביצירת slug.** אם בודקים "האם ה-slug תפוס?" ואז מכניסים, שתי בקשות במקביל יעברו שתיהן את הבדיקה. הפתרון הוא לא lock בקוד — הוא אילוץ `UNIQUE` בבסיס הנתונים ותפיסת שגיאת ה-constraint. הבדיקה והפעולה חייבות להיות אטומיות, וב-Postgres הן כאלה רק אם ה-DB אוכף אותן.

**פרויקט ציבורי לא חושף יותר ממה שהתכוונו.** נתיב הקריאה הציבורי מחזיר כותרת, תוכן וקישורים. הוא לא מחזיר `owner_id`, לא `updated_at` פנימי, ולא רשימת גרסאות. מודל אחד לתשובה ציבורית, מודל אחר לתשובה מאומתת — לא אותו `to_dict()` עם דגל.

**הודעות שגיאה לא חושפות פנימיות.** `404` על פרויקט פרטי של מישהו אחר ו-`404` על פרויקט שלא קיים — אותה תשובה בדיוק. ההודעה למשתמש בעברית וגנרית; ה-stack trace הולך ללוג בלבד.

**ולידציית מספרים.** `position` מגיע מהלקוח. `NaN` עובר כל בדיקת טווח שכותבים בתמימות, כי כל השוואה עם `NaN` מחזירה `false`. הבדיקה מתחילה ב-`Number.isFinite()`, לא ב-`value < 0`.

**סכמות URL — בכל מקום שנוצר בו `href` או `src`.** הוולידציה על `project_links` היא רק חצי מהשטח. הפרסר מייצר `<a href>` מכל `[טקסט](כתובת)` שנכתב בתוך `documents.content`, ובהמשך גם `<img src>` מכל `![alt](כתובת)` — והתוכן הזה מגיע מאותו מקום לא אמין.

זה לא היה תיאורטי: הקוד ב-`index.html` העביר את היעד ישירות ל-`href` בלי שום בדיקה, כלומר `[לחץ](javascript:alert(1))` ייצר קישור פעיל. תוקן בפונקציית `safeHref`, שרצה על כל קישור שהפרסר מייצר.

הכלל בשלושת המקומות — קישורי פרויקט, קישורים ב-Markdown, ותמונות ב-Markdown — זהה: **רשימה לבנה** של `http`, `https` ו-`mailto`, בתוספת נתיבים יחסיים ועוגנים. רשימה שחורה תמיד מפספסת סכמה, ו-`data:` ו-`vbscript:` הם רק אלה שזוכרים.

שתי מלכודות שקל ליפול בהן: תווי בקרה בתוך הסכמה (`java\tscript:`) — דפדפנים מתעלמים מהם בפענוח, ולכן צריך להסיר אותם לפני הבדיקה. ואותיות גדולות (`JaVaScRiPt:`) — הבדיקה חייבת להיות case-insensitive.

תמונות מרונדרות היום כטקסט placeholder ולא כ-`<img>`, ולכן הן לא פגיעות כרגע. ברגע שיוצג `src` אמיתי, אותו `safeHref` חייב לחול גם עליהן.

**XSS דרך Markdown.** הפרסר בונה אלמנטים של React ולא מזריק HTML — חוץ ממקום אחד, `dangerouslySetInnerHTML` עבור פלט Prism, ושם Prism כבר עושה escape. אבל הלייב פריוויו מרנדר טקסט שנכתב עכשיו, בלי שעבר שום שרת. אם אי פעם נוסיף תמיכה ב-HTML גולמי בתוך Markdown, זה הופך לחור אמיתי — ואז sanitizer, לא regex.

**מחיקה מדורגת.** `ON DELETE CASCADE` על כל המפתחות הזרים, כך שמחיקת פרויקט לא משאירה מסמכים, גרסאות או קישורים יתומים.

---

## שלבים

שבעה שלבים. כל אחד מסתיים במשהו שאפשר להשתמש בו — לא בחצי תשתית.

### 1. תשתית
Postgres ב-Render עם `Asia/Jerusalem` ו-UTF8, שרת FastAPI, SQLAlchemy async, מיגרציות Alembic, בדיקת בריאות. `render.yaml` עובר מ-`runtime: static` ל-`runtime: python`, והפרונט מוגש מאותו שירות כקבצים סטטיים — כך אין CORS ואין דומיין שני. בנוסף: כל ספריות הצד-שלישי ירדו ל-`assets/vendor/` — Prism עם חבילת שפות ארוזה מראש במקום ה-autoloader, וכן mermaid ו-React. גם הגופנים ירדו, ל-`assets/fonts/`, ואיתם נסגר המקור החיצוני האחרון: ה-CSP כבר לא מכיל אף דומיין זר באף הנחיה.
**המדד:** `/api/health` מחזיר 200 מ-Render, המיגרציות רצות בדיפלוי, ו-`scripts/check-offline.js` עובר — הדף מתפקד במלואו כשכל בקשה חיצונית חסומה.

### 2. כניסה
seed idempotent למשתמש היחיד מ-`ADMIN_EMAIL`/`ADMIN_PASSWORD`, bcrypt, cookie חתום עם `HttpOnly`+`Secure`+`SameSite=Lax` הנושא `session_version` ו-`exp`, rate limiting על `login`, בדיקת `Origin` על בקשות משנות, ומגבלת גודל גוף לפני הפרסור.
**המדד:** בקשה בלי cookie מקבלת 401; cookie שה-`exp` שלו עבר נדחה גם כשהחתימה תקפה; העלאת `session_version` מנתקת cookie קיים; `POST` בלי `Origin` או עם `Origin` זר מקבל 403; שישה כישלונות ברצף מפעילים backoff; גוף מעל 1MB מקבל 413 בלי שנקרא במלואו.

### 3. פרויקטים ומסמכים
ה-CRUD המלא, הגרסאות, נתיב הקריאה הציבורי. בלי ממשק — `curl` בלבד.
**המדד:** יצירת פרויקט, כתיבת מסמך, עריכה, וראייה שהגרסה הקודמת נשמרה. פרויקט ציבורי נקרא בלי cookie; פרטי מחזיר 404.

### 4. חיפוש וקישורים
`tsvector` עם משקלים, `strip_nikud`, `pg_trgm` לחיפוש מטושטש. CRUD לקישורים עם ולידציית סכמה.
**המדד:** חיפוש "התקנה" מוצא את המסמך גם אם נכתב מנוקד, וכותרת מנצחת תוכן בדירוג.

### 5. הממשק
זה השלב שנוגע ב-`index.html` הקיים. `localStorage` מוחלף ב-`fetch`, נוסף מסך פרויקטים, הסיידבר מציג את מסמכי הפרויקט הנוכחי, והקישורים בתחתית עמוד הפרויקט. שני העיצובים ממשיכים לעבוד — המסכים החדשים משתמשים באותם משתני CSS.
**המדד:** יצירת פרויקט מהדפדפן, פתיחה ממכשיר אחר וראיית אותו תוכן.

### 6. העורך
מסך מפוצל, לייב פריוויו עם debounce, סרגל הלחצנים, שמירה אוטומטית, וזיהוי RTL בבלוקי קוד.
**המדד:** הקלדה במסמך של אלף שורות לא מקרטעת; לחיצה על כפתור עם טקסט מסומן עוטפת אותו ולא דורסת; בלוק עברי בלי שפה מתיישר לימין, ובלוק ```js עם הערות עבריות נשאר משמאל.

### 7. הגירה וגיבויים
העלאת המסמכים שב-`localStorage` לשרת, `GET /api/backup.zip`, ואחריו הגיבוי המתוזמן לדיסק הקבוע עם מחיקת ישנים.
**המדד:** אחרי ההעלאה, ניקוי נתוני האתר לא מוחק כלום. ה-ZIP נפתח ומכיל את כל המסמכים. אחרי 31 גיבויים, הישן ביותר נמחק.

---

## מה לא נכנס לגרסה הזאת

עריכה משותפת בזמן אמת, הרשאות ברמת המסמך, ייצוא ל-PDF, תגובות, וסנכרון דו-כיווני עם Git.

כל אלה סבירים בהמשך. אף אחד מהם לא נדרש כדי שהמערכת תהיה שימושית.

---

## שחזור

גיבוי שלא ניסית לשחזר ממנו הוא לא גיבוי — הוא תקווה. לכן `scripts/restore.py`
הוא חלק מהמנגנון ולא נספח לו, ויש בדיקה שסוגרת את המעגל: יצירה → גיבוי →
מחיקת הכול → שחזור → השוואה. **היא מצאה שני באגים אמיתיים ברגע שנכתבה**,
ושניהם היו שקטים: הפרויקט והמסמכים נוצרו מחדש תחת slug שנגזר מהשם ומהכותרת
במקום זה שנשמר. השחזור היה "מצליח", וכל כתובת שנשלחה למישהו הייתה נשברת.

### שלושת מצבי הכתיבה

| מצב | מה קורה | מתי |
|---|---|---|
| `skip` (ברירת מחדל) | כותב רק מה שלא קיים | מסד ריק, מיגרציה, סביבת בדיקה. לא יכול להזיק |
| `upsert` | דורס את מה שבגיבוי, משאיר מה שנוצר אחריו | שחזור נקודתי — "מחקתי בטעות מסמכים אתמול" |
| `replace` | מוחק כל פרויקט שבגיבוי ובונה מחדש | שחזור מלא אחרי אובדן. **מוחק מה שנוצר אחרי הגיבוי** |

לפני שנוגעים בכלום מוצג בדיוק מה עומד לקרות, והאישור נשאל עם ברירת מחדל
`N` — Enter בטעות לא משחזר כלום. `--dry-run` מציג בלי לכתוב.

### תרגיל שחזור

חמש דקות ברבעון. הפעם היחידה שבה תגלה שהגיבוי שבור היא הפעם שבה זה כבר לא
משנה.

1. הורד גיבוי מהכפתור בסרגל.
2. הרם עותק מקומי של המערכת מול בסיס נתונים ריק.
3. `python3 scripts/restore.py backup-XXX.zip --url http://127.0.0.1:8000`
4. פתח את האתר וּודא שהתוכן נראה נכון.
5. מחק את בסיס הנתונים המקומי.

### נראות

`GET /api/backup` מחזיר את שלוש השאלות שחשובות — האם המנגנון פעיל, מתי רץ
לאחרונה, ומתי הבא — ואת רשימת הקבצים. בממשק יש פאנל שמציג אותן ולחצן
"גיבוי עכשיו". `POST /api/backup/run` מוגן בנעילה, והבדיקה `is_running()`
נעשית **לפני** התפיסה: בקשה שנייה חוזרת מיד עם 409 במקום להמתין לסיום ואז
להריץ גיבוי נוסף. תפוס אינו שבור, והמשתמש צריך לדעת מה מהשניים.

הנעילה היא בתוך התהליך. השירות מוגבל למופע יחיד ממילא בגלל הדיסק, ולכן זה
מספיק — ריצה בכמה workers הייתה דורשת נעילה מבוזרת.

---

## תמה שלישית — dim

`docs/Third_design.html` נוסף כערכה שלישית: אפור חמים עם אקסנט ענבר,
Rubik ו-Source Code Pro. היא שייכת למשפחת הכהים ולא לבהיר — הגיאומטריה
שלה זהה כמעט לגמרי ל-dark, וההבדל הוא הטמפרטורה.

**מה שהיה צריך להשתנות מבנית, ולא רק להתווסף.** המערכת הניחה בשני מקומות
ששתי תמות קיימות בלבד, ובשניהם הכשל היה שקט:

`const ed = this.state.theme === "light"` הזין 24 הסתעפויות של צורות
בלוקים. השאלה האמיתית היא "האם זה העיצוב ה-Editorial", והניסוח כהשוואת
שם הכריח כל תמה עתידית לזכור להיות "לא light". עכשיו התמה מצהירה
`editorial: true/false` והקוד שואל אותה.

`root.style.colorScheme = mode` היה מקבל `"dim"` — ערך שאינו חוקי
ל-`color-scheme`. הדפדפן מתעלם ממנו, ופסי הגלילה, פקדי הטפסים וההשלמה
האוטומטית נופלים למשפחה ההפוכה. כל תמה מצהירה עכשיו `scheme`.

שני השדות האלה מסוננים ב-`applyTheme` לפני כתיבת משתני ה-CSS. בלי זה היו
נוצרים `--scheme` ו-`--editorial` שאיש לא קורא, והשני היה המחרוזת
`"false"` — ערך שנראה אמיתי בכל בדיקה.

`CHROME.dim` נגזר מ-`CHROME.dark` ולא מועתק: כל המחרוזות שם בנויות
מ-`var()` וכבר מקבלות את צבעי התמה הפעילה. עותק היה נשאר מאחור בכל שינוי
עיצוב בשתי האחרות.

**הכפתור מחזורי** — dark → dim → light — עם שם התמה הבאה ב-tooltip. פקד
מקטעים היה מצטופף בסרגל שכבר מחזיק חמישה פקדים.

**הגופנים ירדו לריפו** יחד עם השאר. Rubik ו-Source Code Pro היו מחזירים
מקור חיצוני לדף שכבר לא היה בו אף אחד, ו-`check-offline.js` היה נופל.

`scripts/check-themes.js` מוודא שהמעבר באמת מחליף ערכה — צבעים, גופן,
`color-scheme` חוקי, צורות הבלוקים, שרידות רענון, ושם פסול ב-localStorage
שנופל לברירת מחדל. בדיקה על `data-theme` בלבד הייתה עוברת גם אם שום דבר
לא באמת התחלף.

---

## מסך הכתיבה, וניקוי המסך הראשי

### מסך יצירת מסמך = מסך העריכה

הייתה קופסה בסיידבר שמדביקים לתוכה Markdown ולוחצים "הוספה". היא הייתה
ברוחב הסיידבר, בלי פריוויו ובלי סרגל תבניות — כלומר כתיבה אמיתית לא
נעשתה בה אף פעם.

עכשיו הכפתור פותח את **אותו** רכיב `editorPane` של העריכה. אין כאן עורך
שני: `editBuf` משותף, ולכן הפריוויו, סרגל התבניות והמשך הרשימות עובדים
בלי שורה נוספת. מה שמתחלף הוא השורה העליונה בלבד — מחוון השמירה בעריכה,
ושדה שם + "הוספה לפרויקט" + "ביטול" ביצירה.

שלושה דברים שהיו נשברים בשקט אילו רק "מעתיקים את המסך":

- **מיקום ב-DOM.** הקינון הטבעי היה בתוך הבלוק של המסמך הפתוח, ואז מסך
  היצירה לא נפתח כלל בפרויקט ריק — בדיוק המצב שבו צריך אותו. הוא יושב
  ברמת הפרויקט, ו-`onDoc`/`noDoc` נסוגים מפניו.
- **כותרת רמה 1.** `render_blocks` מדלגת עליה תמיד, כי בתצוגת המסמך היא
  מוצגת בכותרת העמוד. במסך היצירה אין כותרת עמוד, והשורה הראשונה של
  הטיוטה פשוט נעלמה מהפריוויו. נוסף `keepH1`, שדולק רק במצב יצירה.
- **הטיוטה חיה בזיכרון בלבד.** מעבר למסמך אחר או לרשימת הפרויקטים היה
  מוחק אותה בלי לומר מילה. `confirmLeaveCreating()` שואל, ורק כשיש תוכן.

`startCreating` מבריח שמירה ממתינה לפני שהוא מרוקן את ה-buffer, כי
ה-buffer משותף עם העורך.

### כרטיס הפרויקט

היה שלוש שורות בגובה קבוע: שם, `"בלי תיאור."`, ומטא. שורת ה"בלי תיאור"
תפסה מקום כדי לומר שאין מה לומר. עכשיו השם, המטא והתג על קו אחד, והתיאור
הוא מחרוזת ריקה כשאין — `span` ריק מתכווץ לגובה אפס. כרטיס בלי תיאור הוא
שורה אחת.

בטופס היצירה נוסף שדה תיאור. ה-API תמך בו מהיום הראשון; רק הממשק לא
שאל. שדה ריק אינו נשלח בכלל — היעדר תיאור ו"תיאור ריק" הם שני דברים.

### `refreshProject` עדכן חצי מהמצב

הפרויקט הפתוח התרענן, הרשימה לא. מונה המסמכים בכרטיס נשאר על הערך שהיה
בטעינת הרשימה עד לרענון הדף. שני העותקים מגיעים מאותו payload, ולכן שניהם
מתעדכנים מאותה תשובה.

### פרטים

`docCountLabel` במקום `n + " מסמכים"` בשני מקומות: `"1 מסמכים"` נקרא
כשגיאה. `editorPane` איבד פרמטר `leadIdx` שכבר לא היה בשימוש אחרי שהפריוויו
עבר ל-`skipIdx = -1` — פרמטר שקרי מזמין קריאה שגויה של הקוד.

### תיקוני ביקורת למסך הכתיבה

ארבעה מסלולים מוחקים את הטיוטה, ורק שניים מהם עברו באישור. `openCreate`
בזמן שהמסך כבר פתוח, ו-`cancelCreating`, איפסו את ה-buffer בשקט. השאלה
הופרדה מהפעולה — `mayDiscardDraft()` שואל בלבד, ו-`confirmLeaveCreating()`
בונה עליו — וכל ארבעת המסלולים עוברים דרכה.

`resetEditor()` מבטל את `previewTimer` לפני האיפוס. הטיימר מחזיק את הערך
הישן ב-closure, ופריקה שלו אחרי מעבר למסך היצירה החזירה את תוכן המסמך
ל-`editView` — תיבה ריקה מול פריוויו של מסמך אחר. `flushSave` ביטל רק את
טיימר השמירה.

`submitDraft` שמר `editBuf.trim()`. trim משמש עכשיו רק כדי לדעת אם יש
תוכן; מה שנשמר הוא מה שנכתב.

**המשך רשימה באותיות עבריות** נשען על `charCodeAt(0) + 1`. בטבלת יוניקוד
הסופיות יושבות בין האותיות, ולכן `י` המשיך ל-`ך`, `ל` ל-`ם`, ו-`פ` ל-`ץ`.
`HEB_ALPHABET` הוא הרצף לפי סדר הקריאה.

**החלטת הריקנות** נבחנה רק על מה שלפני הסמן. `"1. |טקסט"` נראה משם כמו
פריט ריק, והיציאה מהרשימה מחקה את הסימון והשאירה את הטקסט מרחף. עכשיו
מה שלפני הסמן קובע רק אם הסמן עבר את הסימון, והריקנות נקבעת לפי השורה
כולה.

`render_blocks` קיבל אובייקט אפשרויות במקום ארבעה פרמטרים, שלושה מהם
בוליאניים — סדר מוחלף ביניהם היה עובר בשקט בכל בדיקה.

### טבלאות רחבות

טבלה רחבה נחתכה. שני גורמים נפרדים, ושניהם נראו כמו עיצוב תקין:

`width:100%` על הטבלה לא מונע גלישה, הוא רק דוחס. טבלה בת שמונה עמודות
נדחסה עד שאי אפשר לקרוא אותה, ותא שמכיל קוד או כתובת שאי אפשר לשבור גלש
החוצה בכל זאת. ובענף הלא-Editorial המעטפת נשאה `overflow:hidden` — היא
נועדה לחתוך את הפינות לפי ה-`border-radius`, וחתכה בדרך גם את העמודות
האחרונות, בלי שום סימן שהן קיימות.

הפתרון הוא זה שכבר היה בבלוקי הקוד ובתרשימי Mermaid: מעטפת עם
`overflow-x:auto`. הטבלה מקבלת `width:max-content` עם `min-width:100%` —
רחבה כפי שהיא צריכה כשיש הרבה עמודות, ונמתחת לרוחב המלא כשאין.

`max-width` על התאים מונע את הקצה השני: עמודה של פסקאות הייתה נמתחת
לאלפי פיקסלים במקום לשבור שורה. הערך תלוי גם ברוחב המסך
(`min(420px, 55vw)`) ולא רק בקבוע, כי 420px על מסך של 360px היו מכריחים
גלילה על טבלה של שתי עמודות שיכולה פשוט לשבור שורה.

### `scripts/check-syntax.js`

הקוד של הרכיב חי בתוך `<script>` ב-`index.html`, ו-dc-runtime מריץ אותו
דרך `new Function`. שגיאת תחביר שם אינה מפילה את הדף: ה-runtime תופס
אותה, מדפיס אזהרה לקונסולה, ומרנדר את התבנית עם props בלבד — מסך שנראה
כמעט תקין ולא מגיב לכלום.

בדיקות הדפדפן כן תופסות את זה, כי הן נכשלות על שגיאת קונסולה. אבל הן
דורשות שרת, מסד ודפדפן, ולוקחות דקה. הבדיקה הזאת עושה את אותו הדבר על
סוגר חסר בפחות משנייה, ומדווחת את מספר השורה בקובץ עצמו.

הגרסה הראשונה עוגנה ב-`class Component extends DCLogic`. זה נראה מדויק
יותר, והיה חור: כל מה שמעל המחלקה באותו בלוק — הערכות, `CHROME`,
`SNIPPETS`, `MERMAID`, הקבועים — לא נבדק כלל, בעוד ששגיאת תחביר שם
שוברת את הדף בדיוק באותה דרך. העוגן הוא עכשיו תגית ה-`script` עצמה,
והכיסוי עלה מ-1954 שורות ל-2396.

### תרשימי Mermaid — טקסט על רקע שלא נצבע

`paintSvg` הכירה רק צורות של flowchart: `g.node rect`, `g.cluster rect`.
תרשימי ER ו-sequence משתמשים במחלקות אחרות לגמרי, ולכן הקופסאות שלהם
נשארו עם הפלטה **הבהירה** המובנית של Mermaid — בעוד שהשורה הבאה בקוד
כפתה על כל טקסט את צבע הטקסט של הערכה. בתמות הכהות התוצאה הייתה לבן
על לבן; בבהירה זה נראה תקין במקרה, כי הטקסט כהה ממילא.

מה שנמדד בתמה הכהה לפני התיקון:

| אלמנט | fill |
|---|---|
| `rect.er.attributeBoxOdd` | `#ffffff` |
| `rect.er.attributeBoxEven` | `#f2f2f2` |
| `rect.er.entityBox` | `#fff4dd` |
| `rect.actor.actor-top` | `#fff4dd` |
| `text.er.entityLabel` | `#e9edf7` — נצבע על ידינו |

התיקון מוסיף את הסלקטורים של שתי המשפחות וממפה אותם לטוקנים שכבר קיימים
בערכה: כותרת הישות ל-`node`, שורות מתחלפות ל-`surface` ו-`cluster`, תווית
הקשר ל-`edgeBg`. אין טוקן חדש. צביעת הטקסט הועברה לסוף במפורש, כדי שלא
יהיה שלב שבו טקסט נצבע מעל רקע שנשאר בברירת המחדל.

`scripts/check-mermaid.js` מרנדר תרשים מכל משפחה — flowchart, ER, class,
state, sequence — בשלוש הערכות, ומחשב לכל טקסט את יחס הניגודיות מול
הצורה הצבועה שמתחתיו. הבדיקה שהייתה קיימת ספרה כמה SVG נוצרו, ותרשים
שכולו לבן על לבן עבר אותה בשלמות. במוטציה שהחזירה את הבאג היחס שנמדד
היה 1.07 — כלומר אותו צבע כמעט בדיוק.

### מסך מלא

כפתור אייקון בשורת המטא של המסמך. Fullscreen API על מכל המסמך ולא על
`documentElement`, ולכן הסיידבר והסרגל העליון יוצאים מהמסך.

שני דברים שקל לפספס. הראשון: יציאה ב-Esc או במחוות הדפדפן אינה עוברת דרך
הכפתור, ולכן `fullscreenchange` הוא מקור האמת ולא מה שהכפתור חשב. השני:
Safari ב-iOS אינו תומך ב-`requestFullscreen` על אלמנט כלשהו, ושם הכפתור
היה נלחץ ולא קורה כלום — לכן יש נפילה למצב מיקוד ב-CSS, עם `Escape`
משלו כי אין דפדפן שיטפל בו.

רקע וריפוד נדרשים גם במסלול ה-API האמיתי: אלמנט שנכנס למסך מלא מצויר מעל
backdrop שחור, ובלי רקע משלו הטקסט מרחף על שחור בכל ערכה.

---

## תמה רביעית — Coast, בשתי בהירויות

`docs/Fourth_design.md` שונה משלושת קודמיו: הוא מפרט טוקנים ולא מוקאפ
מרונדר, והוא מגדיר **פלטה בהירה ופלטה כהה לאותה זהות**. לכן זו לא ערכה
אחת אלא שתיים — `coast` ו-`coast-dark` — ושתיהן במשפחת ה-Editorial.
`THEME_ORDER` הוא עכשיו בן חמישה, ושתיהן יושבות בסוף וצמודות זו לזו כי
הן נבדלות רק בבהירות.

**הבסיס שוחזר, לא הומצא.** המפרט מרחיב "Coast 0.1.2" שאינו בריפו,
ומפנה לטוקנים כמו `{colors.on-surface}` ו-`{colors.surface-container}`
שאין להם ערך בקובץ. מקטע ה-Mermaid מדליף אותם: הרקע (`#f9f4e8`),
האוקיינוס (`#1e5f8c`), הים-קצף (`#5ba8b8`) והדיו (`#2a3540`), ובכהה
`#0a1828` ו-`#142a40`. ה-frontmatter נותן את השאר.

שבעת הגוונים של המפרט ממופים לשמות הגוונים הקיימים — מידע→`accent`,
הצלחה→`green`, אזהרה→`amber`, סכנה→`red`, הדגשה→`purple`, טיפ→`teal`,
מעומעם→`gray` — כדי שטבלת `CALLOUT` תמשיך לעבוד בלי שינוי. הרקעים
הכהים הם גוני-כמעט-שחור של הגוון ולא הבהרה של המשטח, כהנחיית המפרט:
התראה בתוך תצוגה כהה לא אמורה לזהור.

**הגופנים כבר היו בריפו.** Frank Ruhl Libre ו-Heebo משרתים את light,
ו-IBM Plex Mono הוא הבחירה השנייה במחסנית ה-mono של המפרט עצמו. לכן לא
ירד אף גופן חדש ו-`check-offline` נשאר ירוק.

`CHROME.coast` נגזר מ-`CHROME.light` עם שני שינויים שהמפרט דורש: תוויות
בעברית במקום ה-wordmark האנגלי, ו-`uppercase` יורד. לעברית אין רישיות,
ולכן ההמרה לא עושה כלום לעברית וצועקת בלטינית — ומייצרת רמת כותרת
שנראית שונה בין שתי השפות באותה שורה.

### שלושה באגי RTL שהמפרט חשף, וכולם היו בכל הערכות

**קו חוצה על משימה שהושלמה.** בלטינית ה-ascenders שורדים מעל הקו; בעברית
אין אותיות עולות, והקו חותך בדיוק את רצועת ה-x-height שנושאת כמעט את כל
ההבחנה בין האותיות. הצבע המעומעם כבר נשא את המצב ממילא.

**`box-decoration-break: slice`** על `<mark>` — הדגשה שנשברת בין שתי
שורות קיבלה ריפוד ופינות רק במקטע הראשון ובאחרון.

**קו תחתון שנבנה מ-`border-bottom`.** border נמתח לכל רוחב תיבת התוכן
ואינו יודע דבר על הכתב שמעליו. `text-decoration` כן: `skip-ink` שובר
את הקו סביב ק, ן, ץ, ך, ו-`underline-offset: 0.25em` מרחיק אותו מהן
מלכתחילה.

### תיבת סימון אמיתית

`span` עם ✓ נראה זהה ואינו נגיש — קורא מסך אינו מכריז עליו כתיבת סימון
ואינו יודע לומר אם היא מסומנת. עכשיו זה `<input type="checkbox">` עם
`appearance: none`, כלומר המראה שלנו והסמנטיקה של הדפדפן. ברינדור בתוך
מכולה התיבה `disabled` ולא `pointer-events: none`, כדי שהמצב עדיין
יוכרז.

### `check-themes.js`

שתי בדיקות המחזור השוו ל-3 קבוע. זו בדיוק הבעיה שהתגובה שמעליהן
מזהירה ממנה, רק בכיוון ההפוך: תמה רביעית עם כותרת תקינה לגמרי הייתה
מפילה אותן, ותמה רביעית שחולקת כותרת עם אחרת הייתה עוברת. המספר נגזר
עכשיו מ-`THEME_ORDER`. `check-mermaid.js` עבר משלוש ערכות לחמש.

הסמלים ⛵ ו-⚓ נפסלו: הם `Emoji_Presentation` כברירת מחדל, כלומר הדפדפן
מצייר אותם בצבע קבוע משלו, והם הופיעו כשתי תמונות צבעוניות בשורה שכולה
סמלים חד-גוניים. ◇ ו-◆ הם אותה צורה בשני משקלים — בדיוק מה ששתי ערכות
Coast הן זו לזו.

### תיקוני ביקורת לתמה הרביעית

**טבעת מיקוד.** `appearance: none` על תיבת הסימון מוחק גם את טבעת
המיקוד של הדפדפן, ומסלול המקלדת דרך רשימת משימות היה בלתי נראה.
נוסף כלל `:focus-visible` גלובלי — לא רק לתיבות, כי אותה בעיה קיימת
בכל פקד שהעיצוב מסיר ממנו את המסגרת.

הצבע הוא `--accent` ולא `--accent-ring`. השני הוא אלפא 30%, ומעל
משטח בהיר הוא יוצא ביחס של 1.6:1 — טבעת שקיימת בקוד ואי אפשר לראות
אותה. `--accent` המלא עובר 3:1 בכל חמש הערכות (4.07 עד 6.84).

**דף הרפרנס נשאר מאחור.** שלוש משימות הדוגמה שם היו עדיין `span`
לחיצים עם קו חוצה — כלומר הדף שכל תפקידו להדגים את הרינדור הנכון
הדגים את שתי הבעיות שזה עתה תוקנו. הן עוברות עכשיו דרך אותו
`taskBox`, שהוכלל לקבל את הפעולה במפורש במקום לגזור אותה.

**ניגודיות של משימה שהושלמה.** אחרי הסרת הקו החוצה, הצבע הוא האות
היחיד — ולכן הוא חייב להיקרא. `--faint-2` על כרטיס ב-coast נותן
2.97:1, מתחת ל-AA וגם מתחת ל-AA לטקסט גדול. `--soft` נותן 7.22:1.

**`data-props` לא הכיר את התמות החדשות.** הרנדרר קיבל את `coast`,
המטא-דאטה המוצהרת עדיין הכריזה `auto|dark|dim|light`, ושום בדיקה לא
הביטה בפער. זה לא באג בקוד אלא חוזה שנשבר בשקט: ה-enum בעורך והטיפוס
נגזרים משם. `check-themes.js` מוודא עכשיו ש-`options` מכיל את כל
`THEME_ORDER`.

---

## שינוי שם למסמך

השם וה-slug נעים יחד. ה-slug אינו רק מזהה פנימי — הוא מה שמוצג כשם
הקובץ בשורת המטא (`installation.md`) ומה שיורד בהורדת המקור, ולכן שינוי
שם שלא נוגע בו משאיר את הקובץ בשם הישן ונראה חצי-עבד.

**זה בטוח כאן, ולא היה בטוח בכל אפליקציה.** ל-slug של מסמך אין כתובת
שאפשר לשמור: אין ניתוב לפי hash, ו-`openProject(slug, docSlug)` מקבל
פרמטר שני ששום קורא לא מעביר. ה-slug חי בזיכרון ובנתיב ה-API בלבד.

**השורה שמתחילה ב-`#` לא משתנה.** היא תוכן המסמך, לא שמו. אחרי שינוי שם
הסיידבר ושורת ה-`.md` מציגים את השם החדש, והכותרת הגדולה בעמוד ממשיכה
להציג את מה שכתוב ב-`#` — כי שם היא נגזרת ממנו.

### הגזירה נעשית בשרת

הלקוח שולח `title` ו-`slug_from_title: true`, ולא גוזר בעצמו. כללי
ה-slug נשענים על מחלקות תווים של יוניקוד: `\w` ב-Python תחת `re.UNICODE`
כולל אותיות בכל כתב, ואילו `\w` ב-JavaScript הוא ASCII בלבד. תאום בצד
הלקוח היה נראה נכון ופוסל שמות עבריים לגיטימיים — `מדריך הפעלה` היה
יוצא ריק.

הדגל מפורש ולא גזירה אוטומטית מכל `title` שנשלח. השמירה האוטומטית של
העורך אינה שולחת כותרת היום, אבל אם מישהו יוסיף אותה, גזירה שקטה הייתה
משנה את שם הקובץ בכל הקלדה.

התנגשות נתפסת ב-`UniqueConstraint("project_id","slug")` בזמן ה-flush
ולא בבדיקת SELECT שקודמת לכתיבה — מאותה סיבה שבה היצירה עושה את זה
(כלל 2). התשובה היא 409 עם אותה הודעה עברית.

### `docSlug` הוא הנקודה החדה

אחרי שינוי שם צריך להעביר את `s.docSlug` לחדש. הוא מזין את הנתיב של כל
שמירה הבאה ואת סימון המסמך הפעיל בסיידבר — אילו היה נשאר על הישן,
השמירה האוטומטית הבאה הייתה מקבלת 404 ומה שהוקלד היה הולך לאיבוד בלי
שום סימן. `renameDocument` מבריח קודם את השמירה הממתינה, כי היא צריכה
לצאת לנתיב הישן בזמן שהוא עדיין קיים.

`check-editor.js` מודד את שלושת הדברים בנפרד: שורת ה-`.md` זזה, השורה
שמתחילה ב-`#` לא, וטקסט שנכתב אחרי השינוי נמצא בנתיב החדש.

### תחרות בהחזרת הסמן

בדרך התגלה באג שלא קשור לשינוי השם ושהיה בקוד מאז שנוסף המשך הרשימות.
אחרי כתיבה לתיבה, הסמן הוחזר למקומו דרך `requestAnimationFrame` — כלומר
בערך 16ms אחרי הכתיבה, לפני הציור הבא.

מי שמקליד מהר, או לוחץ Enter וממשיך מיד, הספיק להכניס תווים לפני שה-rAF
פרק. אז הסמן קפץ אחורה, והתווים שכבר נכתבו נשארו במקום אחר. `פריט שני`
יצא `ט שניפרי`.

זה נראה כמו פליקיות בבדיקות ולא כמו באג, כי הוא תלוי-זמן ומופיע רק
לפעמים. שתי הרצות נכשלו ככה קודם ויוחסו לעומס — ובפעם השלישית load
average היה 0.10, מה שפסל את ההסבר הזה.

`componentDidUpdate` רץ מיד אחרי ש-React סיים לכתוב ל-DOM, באותה משימה,
ולכן אין חלון שבו אירוע מקלדת יכול להידחף פנימה. שלוש נקודות ההחזרה —
המשך רשימה, יציאה ממנה, והזרקת תבנית מהסרגל — עברו למנגנון אחד.

---

## שבירת שורות בתוך פסקה

שורות שנכתבו אחת מתחת לשנייה נדבקו לפסקה אחת, גם בתצוגה המרונדרת בעורך
וגם במסמך עצמו. השורש היה בפרסר ולא בתצוגה:

```js
if (para.length) blocks.push({ t: "p", text: para.join(" ") });
```

ה-newline נזרק כבר בזמן הפרסינג, ומאותו רגע שום דבר בהמשך השרשרת לא
יכול היה להחזיר אותו. שני המסכים הראו את אותו הדבר כי הם אותו renderer.

זו התנהגות CommonMark תקנית — שורה בודדת היא רווח, ושבירה אמיתית דורשת
שני רווחים בסוף השורה. הכלל נכתב לטקסט שגולל ידנית ב-80 תווים, ואף אחד
לא מקליד שני רווחים בסוף שורה: הם בלתי-נראים לגמרי, וגם עורך שמראה
רווחים לבנים לא מבדיל בין אחד לשניים. GitHub, Obsidian וטלגרם כולם
נטשו את הכלל הזה מאותה סיבה. אותו `join(" ")` היה גם בציטוטים.

### הפיצול ב-inline ולא ב-render_blocks

ה-`\n` נשמר בבלוק, ו-`inline` הופכת אותו ל-`<br/>` בשתי הנקודות
שדוחפות טקסט גולמי.

פיצול הפסקה לשורות **לפני** הפרסינג היה נראה פשוט יותר ושובר הדגשה
שנפרסת על שתי שורות: `**מודגש` בשורה אחת ו-`והמשך**` בשנייה מגיעים
כשני מחרוזות נפרדות, כל אחת בלי הכוכביות הסוגרות שלה, ומוצגות כטקסט
רגיל עם `**` גלויות. מאותה סיבה נוסף `\n` לתנאי של `kids()`.

הפתיח בראש העמוד הוא היוצא מן הכלל: הוא נכנס לתבנית כמחרוזת גולמית ולא
עובר דרך `inline`, ולכן קיבל `white-space:pre-line`.

### הבדיקה מודדת שורות ולא תגיות

`check-ui.js` סופר שורות ויזואליות דרך `Range.getClientRects`, ולא
`<br>` ב-DOM. `<br>` שקיים ונבלע ב-CSS נראה לקורא בדיוק כמו `<br>`
חסר, וספירת אלמנטים הייתה עוברת עליו. `Element.getClientRects` לא מתאים
כאן — על אלמנט בלוק הוא מחזיר תמיד מלבן אחד, את תיבת הבלוק.

נמדדים ארבעה דברים: שלוש שורות רצופות נותנות שלוש שורות, שורה ריקה
עדיין יוצרת פסקה נפרדת ולא רק שבירה, הדגשה שנפרסת על שתיים נשארת
`<strong>` אחד, וציטוט רב-שורתי נשבר.

**מה שהשתנה לרעה:** מי שגולל טקסט ידנית לשורות באורך קבוע יראה אותן
שבורות כפי שכתב. בעורך הזה כותבים בתיבה עם גלישה אוטומטית, ולכן זה
כמעט לא קורה בפועל.

### הכוכבית היחידה — מה שה-`\n` הסתיר

הביקורת תפסה רגרסיה אמיתית שנוצרה כאן. החלופה של כוכבית יחידה הייתה
`(\*[^*\n]+\*)`, וה-`\n` בה לא נכתב בשביל שורות: הוא הגביל את הטווח
לשורה אחת, וכך גם מנע במקרה מכוכביות ספרותיות להפוך לנטוי. כל עוד
הפסקה חוברה ברווחים לא היה לזה מחיר, כי נטוי לא יכול היה לחצות שורה
ממילא. מרגע שה-`\n` נשמר, אותה הגבלה חוסמת `*מוטה` שנפרס על שתיים.

התיקון שהוצע — פשוט להתיר `\n` — מחזיר את הבאג שההגבלה הסתירה:
`5 * 3 ועוד * 2` הופך ל-`<em>`. מה שנכנס במקום הוא הכלל של CommonMark
עצמו: הפותחת אינה מלווה ברווח והסוגרת אינה מוקדמת ברווח, כלומר
`\*[^\s*](?:[^*]*[^\s*])?\*`. שני המקרים נבדקים — נטוי על שתי שורות,
וכוכביות ספרותיות שנשארות טקסט.

`**` נשאר כפי שהיה: הוא כבר התיר `\n` מלכתחילה, והוא נדיר מספיק בטקסט
רגיל כדי שלא יהיה שווה לגעת בו.

### code span הוא היוצא מן הכלל היחיד

טקסט של קישור עבר גם הוא דרך מנגנון השבירה, אבל `code` לא, בכוונה:
CommonMark קובע ששבירת שורה בתוך code span היא רווח, וזה גם מה שנכון
לראות — שם של פונקציה שנקטע לשתי שורות בתוך המסגרת נראה כמו שני שמות.

### קיבוץ המלבנים בסובלנות

`Math.round(top)` לא היה עמיד: באותה שורה ויזואלית יושבים כמה מלבנים —
צומת טקסט, `<strong>`, `<code>` — ותיבת inline עם גודל גופן או ריפוד
אחרים מתחילה כמה פיקסלים מעל או מתחת. העיגול היה סופר אותם כשורות
נפרדות. הסף 8px יושב בין הפער הזה (בודדים) לבין מרווח שורה אמיתי (כ-30px).

---

## הפריסה שלא הגיעה למשתמש

באג שהתגלה מהדיווח "אין שינוי" על תיקון שכבר היה בפרודקשן. הקוד החדש
אכן הוגש מהשרת; מה שרץ אצל המשתמש היה הישן.

`index.html` הוגש עם `ETag` ו-`Last-Modified` בלבד, בלי `Cache-Control`.
תשובה בלי הנחיית טריות נופלת ל-RFC 9111 §4.2.2: הדפדפן בוחר לעצמו זמן
חיים היוריסטי, והנפוץ הוא כ-10% מהגיל של הקובץ. קובץ שלא השתנה ארבעה
ימים מקבל כך כעשר שעות של "טרי" — הדפדפן לא שולח בקשה בכלל.

ה-`ETag` שקיים אינו עוזר, וזו הנקודה שקל לפספס: אימות מותנה עובד רק
כשנשלחת בקשה. כשהדפדפן החליט שהעותק טרי, אין למי להשוות אותו.

`Cache-Control: no-cache` אינו "אל תשמור" אלא "שמור, אבל אמת בכל פעם".
הגוף נשאר במטמון, ואימות מול `ETag` שלא השתנה מחזיר `304` ריק.

`/api` לא נכלל: התשובות שם נוצרות בלי `ETag` ובלי `Last-Modified`,
ובלעדיהם אין לדפדפן על מה לבסס היוריסטיקה מלכתחילה. הטסט מוודא את
שלושת המקרים — המעטפת, הנכסים, וה-API שנשאר בלי הכותרת.

## גליפי הסרגל

כפתורי העיצוב הראו אותיות (`B`, `I`, `S`, `H`) וגליפים (`•`, `▤`).
עכשיו כל כפתור מראה את התחביר שהוא מזריק: `~`, `=`, `` ` ``, `-`.
נטוי קיבל `/`, ובלוק הקוד ירש את `</>` שהתפנה מהקוד השורתי.

התחביר עצמו לא זז — `icon` הוא תצוגה בלבד, ו-`before`/`after` נשארו
כפי שהיו. השם המלא בעברית ממשיך לשבת ב-`title`.

`</>` בכפתור עובר בהקשר RTL היפוך כפול — סדר התווים מתהפך וגם `<` ו-`>`
מקבלים גליף מראה — ולכן הוא נראה כמו שנכתב. זה נבדק ולא הונח.

---

## שלוש הרמות שהיו רמה אחת

`####`, `#####` ו-`######` יצאו זהות לגמרי — אותו תג `<h4>`, אותו גודל,
אותו צבע. הפרסר תמיד הבדיל ביניהן, כי `level` נגזר מאורך רצף ה-`#`;
הרינדור הוא שאיחד אותן בענף `else` יחיד שתפס את כל מה שאינו 1, 2 או 3.

זו לא הייתה שכחה טהורה: דף הרפרנס הצהיר "H4 הוא הגבול — מעבר לזה כבר
צריך עמוד נפרד". אבל הפרסר קיבל `######`, נתן לו מזהה עוגן ורינדר אותו,
כלומר האפליקציה קיבלה קלט שהיא לא תמכה בו. המשפט ברפרנס עודכן.

### הסולם נפתח כלפי מעלה

טקסט הגוף הוא 16.5, ו-H4 היה 16 — כלומר כבר קטן ממנו. הקטנה נוספת ל-5
ול-6 הייתה נותנת שתי כותרות בגודל הערת שוליים. לכן H4 עלה ל-18, מעל
טקסט הגוף, ומתחתיו נפתח מקום לשתי רמות אמיתיות.

הגודל אינו הסימן היחיד. כל רמה יורדת גם במשקל (700 → 700 → 600) וגם
בעוצמת הצבע (`--amber`/`--accent` → `--head` → `--soft`), כדי שההיררכיה
תיקרא גם כשהכותרות אינן זו לצד זו וגם למי שאינו מבחין בין הגוונים.

לטבלה שתי עמודות גודל. הערכות הכתבתיות גדולות לאורך כל הסולם — טקסט
הגוף שלהן 17.5 מול 16.5, ו-H3 שלהן 23 מול 20 — ועמודה משותפת אחת
הייתה מציבה שם את H5 מתחת לטקסט הגוף, בדיוק הבעיה שהשינוי בא לפתור.

התג נגזר מהרמה ולא נשאר `h4` קבוע, כדי שקורא מסך ישמע את העומק הנכון.

### מדידה

נמדד בכל חמש הערכות: גודל יורד ממש בין רמה לרמה, ויחס ניגודיות מול
הרקע האפקטיבי. הנמוך החדש הוא 5.53:1, מעל הרף של WCAG AA.

הגרסה הראשונה של המדידה הזאת הייתה שגויה: פונקציית ה-luminance שכתבתי
בה לא בדקה אלפא, ולכן רקע שקוף נקרא כשחור וכל הערכות הבהירות יצאו
סביב 1.2:1 — כולל H3 הקיים, שברור שאינו כזה. תוצאה שמאשימה קוד שלא
נגעתי בו היא סימן לכלי שבור ולא לבאג שהתגלה.

---

## רשימת הגדרות

דף הרפרנס הציג רכיב "רשימת הגדרות" — זוגות של מונח והסבר — שהיה כתוב שם
ידנית כ-HTML, בלי שום תחביר Markdown שמייצר אותו. הדף שאמור להיות
הקטלוג של מה שאפשר לכתוב הדגים משהו שאי אפשר לכתוב.

הקוד עצמו כבר הזהיר מזה. ההערה מעל referenceTasks כותבת שתיבה מזויפת
בדף שכל תפקידו להדגים רינדור היא בדיוק המקום שבו אי-התאמה מתגלה מאוחר,
ולכן רשימת המשימות שם נבנית מהקוד. רשימת ההגדרות הייתה היחידה שנשארה
קפואה, וכעת היא נבנית מ-Markdown אמיתי דרך אותם parse ו-render_blocks.

### התחביר, ולמה הרווח קריטי

התקן של PHP Markdown Extra ו-kramdown: שורת מונח, ומתחתיה שורה שמתחילה
ב-`:` ורווח.

הרווח אינו קוסמטי — הוא מה שמפריד בין `: הגדרה` לבין `:::` של ההתראות.
ענף ההתראות רץ ממילא קודם בפרסר, אבל הסתמכות על סדר הענפים לבדה נשברת
בשקט ברגע שמישהו מזיז אחד מהם, ולכן ההפרדה נמצאת בתבנית עצמה ונבדקת.

הזיהוי הוא לפי השורה **הבאה** ולא לפי הנוכחית: המונח הוא טקסט רגיל
לגמרי, ומה שהופך אותו למונח הוא מה שבא אחריו.

### שלוש נקודות שהיו שוברות בשקט

**לולאת הפסקה עוצרת לפני שורת המונח.** אילו הפסקה הייתה בולעת אותו,
שורת ההגדרה שמתחתיו הייתה נשארת יתומה ומוצגת עם `:` גלויה — כלומר
הבאג היה נראה כמו שגיאת כתיב של המשתמש.

**המונח חייב להיות שורה רגילה.** בלי הבדיקה, `## כותרת` שאחריה שורת
הגדרה הייתה נבלעת לתוך ה-dl, מאבדת את היותה כותרת, והעוגן שלה בתוכן
העניינים היה נעלם איתה.

**שורה ריקה בודדת בין זוגות אינה מסיימת את הרשימה** — בדיוק הכלל שכבר
היה בפרסר הרשימות ל"רשימה רופפת". כלל אחד לשני המקומות ולא שניים
מתחרים.

תבנית "פתיחת בלוק" הייתה כתובה פעמיים — בלולאת הפסקה ובבדיקת המונח —
ואוחדה ל-`BLOCK_START`. שתי עותקות של אותה תבנית נפרדות זו מזו בשקט
ברגע שמוסיפים תחביר.

### מה שנבדק

שבעה מקרים: זוג יחיד, שני זוגות עם שורה ריקה ביניהם שנשארים רשימה אחת,
מונח עם שתי הגדרות, התראה `:::` ששורדת, שורת `:` יתומה שנשארת טקסט,
פסקה רב-שורתית שלא נבלעת, וכותרת שנשארת כותרת.

---

## הפסקה הראשונה שיצאה גולמית

קישור שנכתב `[טקסט](url)` נראה תקין בתצוגה המקדימה של העורך ויצא גולמי
במסמך. זה נראה כמו באג בקישורים ולא היה כזה: בגוף המסמך הקישורים עבדו
מצוין. מה שנשבר היה **מיקום** — הפסקה הראשונה בלבד.

הפסקה הראשונה מקודמת לכותרת העמוד, ושם הוזרקה כמחרוזת גולמית:

```js
lead: firstP ? firstP.text.replace(/[`*~]/g, "") : "",
```

ה-replace מחק רק `` ` ``, `*` ו-`~`, ולכן הסוגריים של קישור שרדו אותו
במלואם והכתובת הופיעה על המסך. אותה פסקה בדיוק נראתה תקינה בעורך, כי
הפריוויו מרנדר עם `skipIdx = -1` ומעביר אותה דרך `render_blocks` ומשם
ל-`inline` — כמו כל פסקה אחרת. פסקה אחת, שני מסלולי רינדור.

הכותרת סבלה מאותו דבר: `title` נגזר מ-`h1.text` ומוזרק ל-`<h1>` באותה
דרך, כך שכותרת עם קישור או הדגשה הופיעה גולמית.

התיקון הוא להעביר את שניהם דרך `inline`. הבינדינג בתבנית נושא רכיבי
React ולא רק מחרוזות — `referenceTasks` ו-`referenceDefs` כבר עושים את
זה.

### שארית שהוסרה יחד עם הסיבה לה

ה-`white-space:pre-line` שהפתיח קיבל כשנוספה שבירת השורות היה עקיפה
שנדרשה בדיוק מפני שהוא היה מחרוזת גולמית. מרגע שהוא עובר דרך `inline`,
ה-`<br/>` נוצר לו כמו לכל פסקה. הצהרה שנשארת אחרי שהסיבה לה נעלמה מטעה
את מי שיקרא אותה, ולכן היא הוסרה — והבדיקה מודדת ששתי השורות עדיין שם.

### תוכן העניינים דורש טקסט ולא רכיבים

פריט בסיידבר יושב בתוך `<a>` ולכן חייב להישאר מחרוזת; רינדור אינו
אפשרות שם. מחיקת תווי הסימון לבדה לא הספיקה — היא הסירה `` ` * ~ = ``
ולא נגעה בקישורים, וכותרת עם קישור הופיעה בניווט עם הכתובת המלאה.
`plainText()` ממיר `[טקסט](url)` ל-`טקסט` ואז מסיר את השאר.

### התבנית שמרנדרת היא גם התבנית שמסירה

הביקורת על `plainText` הצביעה משתי זוויות על אותו שורש, ושתיהן צדקו.

מחיקת התווים — `.replace(/[`*~=]/g, "")` — לא הבחינה בין סימון לבין
תו מילולי. הכותרת "5 * 3 = 15" הופיעה בתוכן העניינים כ-"5  3  15":
הטקסט עצמו אבד, ולא הסימון. זה היה בקוד מלכתחילה והוחמר בכך שנוסף לו
טיפול בקישורים.

הזווית השנייה: שרשרת replace מפרקת קישור עוד לפני שהיא מגיעה ל-code
span, ולכן `` `[טקסט](url)` `` בכותרת הפך ל-`טקסט` בניווט בזמן
ש-`inline` מרנדרת אותו כלשונו. שתי פונקציות, שני כללים, אותה כותרת.

התיקון אינו רשימת replace מדויקת יותר אלא **תבנית אחת** —
`INLINE_MARKUP` — שגם `inline` וגם `plainText` נשענות עליה. `plainText`
עושה מעבר אחד עם אותה חלופיות, ולכן `` ` `` נבדק ראשון בכל מקום
וקישור בתוך code span נבלע כיחידה ומוחזר כלשונו.

התבנית נשמרת **בלי הדגל g**, וכל אתר בונה עותק טרי. `inline` קוראת
ל-`kids` בתוך לולאת ה-`exec` שלה, כלומר נכנסת לעצמה רקורסיבית —
אובייקט משותף עם `lastIndex` משותף היה מדלג על חצי מהטקסט בקריאה
המקוננת. זה היה מתגלה כטקסט שנעלם, לא כשגיאה.

נבדקו תשעה מקרים, כולל `5 * 3 = 15`, קישור בתוך code span, וקישור בתוך
הדגשה. ל-`<aside>` של תוכן העניינים נוסף `data-toc`, כי זיהוי הרשימה
לפי `href^="#h-"` היה נשבר על קישור לגיטימי במסמך שמצביע לעוגן.

---

## שם הקובץ שנקרא הפוך

`סיכום-המצב-במטא-ב-app.md` הוצג כאילו הוא מתחיל מ-`app.md`. שלושת
המקומות שמציגים שם קובץ נשאו `dir="ltr"` קבוע.

`dir="ltr"` מכריח כיוון בסיס משמאל לימין. אלגוריתם ה-bidi מסדר אז את
קטע העברית כיחידה אחת ומניח אותה בצד שמאל, ואת הסיומת הלטינית מימין
לה — כלומר מה שנכתב אחרון מופיע ראשון לעין העברית. שם קובץ כאן נגזר
מכותרת המסמך, ולכן הוא לרוב עברי עם סיומת לטינית: בדיוק המבנה שנשבר.

`dir="auto"` קובע כיוון לפי התו החזק הראשון. נמדד על ארבעה שמות:

| שם | `dir="ltr"` | `dir="auto"` |
|---|---|---|
| `סיכום-המצב-במטא-ב-app.md` | משמאל ✗ | מימין ✓ |
| `מדריך-התחלה.md` | משמאל ✗ | מימין ✓ |
| `דוח-Q3-2026.md` | משמאל ✗ | מימין ✓ |
| `design_system.md` | משמאל ✓ | משמאל ✓ |

השורה האחרונה היא העיקר: התיקון אינו "להפוך ל-RTL" אלא להפסיק לכפות
כיוון. שם אנגלי ממשיך להיקרא בדיוק כמו קודם.

### אותה משפחה ב-code השורתי

`<code>` נשא גם הוא `dir="ltr"` קבוע. שם של פונקציה או מפתח הוא אנגלי
ונקרא משמאל לימין — וזה נשאר נכון תחת `auto` — אבל code span מחזיק גם
הודעות שגיאה ומחרוזות בעברית, ועליהן הכיוון הכפוי הפך את סדר הקריאה.

`unicode-bidi: isolate` נשאר. הוא מונע מהקוד לגרור את הפיסוק של הטקסט
שסביבו, וזה תפקיד נפרד לגמרי מקביעת הכיוון.

### בורר בדיקה שנשען על מאפיין עיצובי

`check-editor` איתר את תג השם דרך `span[dir="ltr"]`, ולכן נפל על שינוי
שאינו קשור אליו כלל — הבדיקה של שינוי שם נכשלה מפני שכיוון הכתיבה
שונה. **בקובץ הזה השתנה הבורר בלבד**, והוא עבר ל-`[data-filename]`.
זיהוי לפי הסיומת `.md` היה מחליף תלות אחת בשנייה — הפעם בפורמט התצוגה
של השם.

**מדידת הסדר הוויזואלי נמצאת ב-`check-ui`**, לא כאן. היא מודדת את מה
שהעין רואה ולא את ערך התכונה, ובשני הכיוונים: שם עברי שמתחיל מימין,
ושם אנגלי שממשיך להתחיל משמאל. השני הוא מה שייפול אם מישהו "יתקן"
בעתיד ל-`dir="rtl"`.

המדידה משווה את התו הראשון לתיבת השורה שלו ולא לתו האחרון. השוואה בין
ראשון לאחרון נכונה רק כשהטקסט על שורה אחת; ברגע שהוא גולש, השניים על
שורות שונות והמיקום היחסי ביניהם מפסיק להעיד על כיוון. לא נמצא קלט
שבו הגרסה הראשונה טעתה בפועל — ההחלפה היא לנכונות מבנייה, לא תיקון
של כשל שנצפה.

---

## ניתוב, וארבעה דברים שנתלו בו

לאפליקציה לא היה ניתוב כלל — לא `hash`, לא `history`, כלום. המצב חי
בזיכרון, ולכן כל רענון החזיר לרשימת הפרויקטים. מאותה סיבה לא הייתה
חזרה מהרפרנס: לא נשמר לאן לחזור.

הכתובת היא עכשיו `#/פרויקט/מסמך`. אין צורך בתמיכת שרת, ולכן היא עובדת
גם בהדבקה ישירה של קישור.

### `#top` אינו ניווט

בעמוד כבר היו עוגנים — `#top` בקישורי הניווט, ו-`#h-6` בתוכן העניינים.
הראוטר מתעלם מכל hash שאינו מתחיל ב-`#/`. בלי ההבחנה הזאת לחיצה על
פריט בתוכן העניינים הייתה נקראת כבקשה לעבור מסך.

### שני אירועים, ולא אחד

הכתיבה היא `pushState` ולא השמה ל-`location.hash`: השמה יורה
`hashchange`, שהיה מפעיל את הראוטר על מה שהראוטר עצמו זה עתה כתב.

בהתחלה הוקשב `popstate` בלבד, וזה נראה נכון — הוא נורה על אחורה/קדימה
וזה מה שצריך. `check-offline` הפילה את ההנחה: `pushState` אינו יורה
`hashchange`, אבל **עריכה ידנית של שורת הכתובת כן**. כלומר מי שמדביק
קישור למסמך בזמן שהאפליקציה כבר פתוחה לא היה מגיע לשום מקום. שני
האירועים מוקשבים, ו-`applyRoute` אידמפוטנטית כדי ששניהם יחד יהיו
בטוחים.

### שתי בדיקות שנשענו על שכחה

`check-offline` ו-`check-backup` עשו `reload` והניחו שהוא נוחת ברשימת
הפרויקטים. זו הייתה הנחה נכונה כל עוד האפליקציה שכחה איפה היא, והיא
נשברה ברגע שהיא הפסיקה לשכוח. שתיהן מאפסות עכשיו את הכתובת במפורש.

ב-`check-offline` נדרשו **שני** שלבים ולא אחד: שינוי hash לבדו אינו
טעינה מחדש, ולכן הרשימה שבזיכרון נשארה זו שנטענה לפני שהפרויקט נוצר.

### שינוי שם משתמש ב-replace ולא ב-push

ה-slug נמצא בכתובת, ולכן שינוי שם חייב לעדכן אותה — אחרת רענון היה
מנסה לפתוח מסמך שכבר אינו קיים בשם הזה. `replaceState` ולא `pushState`,
כי שינוי שם אינו ניווט: שלב היסטוריה נוסף היה מוביל בחזרה לכתובת מתה.

## אישור לפני מחיקה

`×` שיושב צמוד לשם המסמך מחק מיד ובלי ביטול. עכשיו נפתח אישור שמציג
את שם המסמך — אישור שאינו אומר על מה מדובר מאמן ללחוץ "כן" בלי לקרוא,
וזה בדיוק מה שהוא בא למנוע. אותו טיפול לקישורים.

מה שנשמר ב-`state` הוא נתונים בלבד — `kind`, `id` ושם לתצוגה — ולא
פונקציה. אישור שמחזיק פונקציה סגורה על ערכים ישנים מוחק את מה שהיה
נכון בזמן הלחיצה ולא את מה שמוצג עכשיו.

המודאל הוא שכבה מעל המסך ולא החלפה שלו, בשונה ממסך הכניסה: אישור
שמסתיר את מה שעומדים למחוק מכריח לזכור על מה לחצת.

## קישורים חיצוניים בכרטיסייה חדשה

`target="_blank"` על קישורים חיצוניים בלבד. `[חזרה](#סיכום)` שנפתח
בכרטיסייה חדשה היה טוען את האפליקציה מחדש רק כדי לגלול, ומאבד את מה
שנכתב בעורך. ההבחנה נעשית על ה-href שאחרי `safeHref`, כלומר על מה
שבאמת ייכנס לתגית ולא על מה שנכתב במקור.

## אייקון הלשונית

לא היה `favicon` כלל. הקובץ הוא 512×512, והדפדפן מקטין אותו ל-16 או 32
של הלשונית — אין בסביבה כלי תמונות לייצר גדלים ייעודיים, ו-13KB אינם
מצדיקים תלות חדשה. `apple-touch-icon` מצביע לאותו קובץ, כי iOS מתעלם
מ-`rel="icon"` כשמוסיפים למסך הבית.

הבדיקה שולחת את הבקשה במפורש ולא מודדת אותה מתעבורת הדף: דפדפן
headless אינו מושך אייקון בכלל, כי אין לו לשונית להציג אותו בה. היא
בודקת את הכתובת שהתגית **פותרת**, כי תגית שמצביעה לנתיב שגוי נראית
תקינה בקוד ומחזירה 404 בדפדפן — ו-404 של אייקון אינו מפיל כלום, הוא
פשוט לא מופיע.

---

## עריכת קישור, ואייקון שלא רונדר

### הדיסקט שלא קיים

אייקון השמירה היה `🖫` — U+1F5AB, אחד מסימני ה-Wingdings הישנים שנכנסו
ליוניקוד **ולא קיבלו ייצוג אמוג׳י**. כיסוי הגופנים שלו דל, ובאנדרואיד
הוא יצא ריבוע ריק.

לא ניתן היה לשחזר את זה כאן: מחסנית הגופנים במכולה מרנדרת אותו, ולכן
הצילום מהמכשיר הוא המדידה הקובעת. מה שכן אפשר היה לעשות הוא לבחור את
התחליף על בסיס ראיה ולא ניחוש — שלושת האייקונים האחרים באותה שורה
(`✎` `⧉` `⛶`) כן מרונדרים שם, ו-`✎` הוא U+270E מגוש **Dingbats**. לכן
`✓` (U+2713) נבחר מאותו גוש בדיוק.

הוא גם מונוכרום, כלומר יורש את הצבע כמו שלושת שכניו. `💾` היה אמוג׳י
בצבע קבוע וצורם ביניהם.

**הבדיקה מודדת את נקודת הקוד ולא את המראה.** גליף חסר נראה כריבוע
במכשיר אחד וכרגיל באחר, ולכן מה שנבדק הוא שהתו הוא זה שנבחר.

### טופס אחד, שני מצבים

לקישורים לא הייתה עריכה — רק הוספה ומחיקה. במקום טופס עריכה נפרד או
מודאל, כפתור ✎ טוען את הקישור לתוך הטופס שכבר קיים מתחת לרשימה, והכפתור
הופך מ"הוספה" ל"עדכון".

זה אותו שיקול שבו מסך היצירה ומסך העריכה של מסמך הם רכיב אחד: שני
טפסים זהים שנשמרים בנפרד מתפצלים בשקט ברגע שמישהו מוסיף שדה לאחד מהם.

הטעינה לוקחת את `link.url` הגולמי ולא את `href` שעבר `safeHref` —
שמירה של הערך המסונן הייתה כותבת בחזרה כתובת שונה ממה שנשמר.

טעינת קישור לעריכה דורסת מה שהוקלד בטופס בלי לשאול. שני שדות קצרים
שרואים אותם מתמלאים אינם מצדיקים אישור, בשונה מטיוטת מסמך.

### שני tooltips זהים אינם אומרים כלום

הכפתור נוסף עם `title="עריכה"`, וזה בדיוק ה-tooltip של כפתור עריכת
המסמך באותו מסך. הבדיקה נפלה על זה — היא לחצה על העיפרון של הקישור
בזמן שחיפשה את כפתור העורך — וזו הייתה בעיה אמיתית גם למשתמש, לא רק
לבורר. הוא נקרא עכשיו "עריכת הקישור".
