# ROADMAP — מפרויקט בדפדפן למערכת תיעוד

מסמך תכנון. הוא נכתב לפני הקוד, כדי שנסכים על המבנה לפני שמשקיעים בו.

---

## הבעיה

היום המסמכים נשמרים ב-`localStorage` של הדפדפן. זה עובד — הם שורדים רענון, סגירת דפדפן ואפילו דיפלוי חדש — אבל זה נשבר ברגע שעוברים למכשיר אחר. פתחת את האתר בטלפון? הספרייה ריקה. ניקית נתוני אתר? המסמכים נעלמו.

יש גם באג שקט: אם ה-`localStorage` מתמלא, `save()` בולע את השגיאה ואף אחד לא יודע שהמסמך לא נשמר.

המטרה כאן היא לא רק "לשמור בשרת". המטרה היא שיהיו **פרויקטים**, ולכל פרויקט מסמכי Markdown משלו — כלומר להוסיף רמת היררכיה שלא קיימת היום בכלל.

---

## החלטות שהתקבלו

| ההחלטה | מה זה אומר בפועל |
|---|---|
| **משתמש יחיד** | אין הרשמה ואין ניהול משתמשים. כניסה אחת עם סיסמה. טבלת `users` קיימת בכל זאת מיום ראשון, כדי שהוספת משתמשים בעתיד לא תדרוש מיגרציה. |
| **פרויקט יכול להיות ציבורי** | עמודת `visibility` עם `private` או `public`. פרויקט ציבורי נקרא בלי כניסה; כתיבה תמיד דורשת אימות. |
| **האחרון ששומר דורס** | אין נעילה ואין 409. שני טאבים על אותו מסמך — האחרון מנצח. טבלת הגרסאות היא רשת הביטחון. |
| **Markdown ב-Postgres** | עמודת TEXT. לא קבצים ב-Git. |
| **מרחיבים את הפרונט הקיים** | `index.html` נשאר, שני העיצובים נשארים, `localStorage` מוחלף בקריאות API. |

### על מונגו — לא

השאלה הוגנת, והתשובה כאן ברורה יחסית. הנתונים כאן יחסיים לגמרי: משתמש מחזיק פרויקטים, פרויקט מחזיק מסמכים, מסמך מחזיק גרסאות. כל שאילתה מעניינת חוצה את הגבולות האלה.

שלושה דברים שאנחנו צריכים ושמונגו לא נותן בלי עבודה:

אילוץ `UNIQUE (project_id, slug)` הוא מה שפותר את ה-race condition ביצירת מסמך — בלי צורך ב-lock בקוד. חיפוש טקסט מלא בעברית עם דירוג הוא שורה אחת של `tsvector`. ומחיקה מדורגת של פרויקט על כל מסמכיו וגרסאותיו היא `ON DELETE CASCADE`, לא לולאה שעלולה לקרוס באמצע ולהשאיר יתומים.

מונגו היה מוסיף מסד נתונים שני לתחזק, בלי לפתור שום דבר שפוסטגרס לא פותר. **מסד נתונים אחד.**

---

## הסכימה

חמש טבלאות.

```
users          projects          documents         document_versions    project_links
─────          ────────          ─────────         ─────────────────    ─────────────
id             id                id                id                   id
email          owner_id ────┐    project_id ──┐    document_id ──┐       project_id ──┐
password_hash  slug         │    slug         │    content       │       title        │
created_at     name         │    title        │    created_at    │       url          │
               description  │    content      │                  │       position     │
               visibility   │    position     │                  │       created_at   │
               created_at   │    created_at   │                  │                    │
               updated_at   │    updated_at   │                  │                    │
                     └──────┘          └──────┘           └───────┘             └──────┘
```

### למה `slug` ולא רק `id`

כי הכתובת `/p/nimbus-docs/getting-started` קריאה, ואפשר לשלוח אותה למישהו. הכתובת `/p/7/42` היא לא. ה-`slug` ייחודי בתוך הפרויקט, לא גלובלית — כלומר לשני פרויקטים מותר שיהיה מסמך בשם `installation`, וזה בדיוק מה שנרצה.

האילוץ בבסיס הנתונים: `UNIQUE (project_id, slug)`.

### למה `position` ולא סתם סדר לפי תאריך

תיעוד הוא לא בלוג. הסדר של המסמכים בסיידבר הוא החלטה של הכותב — "התקנה" בא לפני "הגדרות מתקדמות" גם אם נכתב אחריו. עמודת `position` נותנת שליטה ידנית, וגרירה בממשק בהמשך.

### למה טבלת גרסאות

זו הטבלה שקיימת "כדי לא להצטער". היום היא נראית מיותרת. ברגע שתמחק בטעות פסקה שכתבת לפני חודש, היא תהיה הדבר החשוב ביותר במערכת.

היא גם מה שהופך את "האחרון ששומר דורס" להחלטה נוחה במקום מסוכנת. הדריסה לא מאבדת מידע — היא רק מזיזה אותו לטבלה אחרת.

השמירה פשוטה: לפני כל `UPDATE` על `documents.content`, דוחפים את התוכן **הישן** ל-`document_versions`. בלי diff, בלי חוכמות — עותק מלא. מסמך Markdown הוא כמה עשרות קילובייטים; מאה גרסאות שלו הן עדיין כלום מבחינת אחסון.

### קישורי פרויקט

טבלה נפרדת ולא מערך JSON בתוך `projects`, כי אנחנו רוצים לשנות סדר ולמחוק קישור בודד בלי לשכתב את כל הרשומה.

מלכודת אחת שקל לפספס: המשתמש נותן כתובת, והממשק מציג אותה כ-`<a href>`. הכתובת `javascript:alert(1)` היא href חוקי לגמרי מבחינת HTML. **הוולידציה חייבת לאשר רק `http` ו-`https`** — לא לחסום `javascript` ברשימה שחורה, כי `data:` ו-`vbscript:` יעקפו אותה. רשימה לבנה של שתי סכמות, וזהו.

---

## בסיס הנתונים — מה למדנו מהסקיל

`docs/israeli-postgres-toolkit/SKILL_HE.md` רלוונטי כאן, ובארבעה מקומות הוא משנה מה שתכננתי:

**דירוג לפי משקל.** במקום `to_tsvector` שטוח, נותנים לכותרת משקל גבוה מהתוכן. מסמך שהמילה מופיעה בכותרת שלו צריך לצוף מעל מסמך שהיא מוזכרת בו באמצע פסקה:

```sql
setweight(to_tsvector('simple', coalesce(title, '')), 'A') ||
setweight(to_tsvector('simple', coalesce(content, '')), 'B')
```

**ניקוד.** התוסף `unaccent` מסיר דיאקריטיקה לטינית בלבד. על ניקוד עברי הוא לא עושה כלום — `unaccent('שָׁלוֹם')` מחזיר `'שָׁלוֹם'`. אם מישהו יכתוב מנוקד ויחפש בלי ניקוד, לא יהיו תוצאות. הפתרון הוא פונקציית `IMMUTABLE` שמסירה את הטווח `U+0591–U+05C7` במפורש, ושימוש בה גם בעמודה המחושבת וגם בשאילתה.

**Collation.** אם נמיין כותרות פרויקטים בעברית לפי ICU, שים לב ש-collation לא דטרמיניסטי לא יכול לגבות `UNIQUE` או אינדקס `btree`. כלומר ה-`slug` נשאר בהשוואה רגילה, ורק המיון לתצוגה משתמש ב-ICU.

**אזור זמן וקידוד.** `Asia/Jerusalem` ו-`UTF8` נקבעים ביצירת בסיס הנתונים. תיקון מאוחר של אחד מהם דורש מיגרציית נתונים, ולכן זו בדיקה של שלב 1 ולא של שלב 5.

הסקיל המלא כבר בריפו, ו-`scripts/hebrew-search-setup.sql` שבתוכו הוא כמעט העתק-הדבק לשלב 4 — הוא כבר מכיל את `strip_nikud`, את `pg_trgm`, ואת ה-`search_vector` עם המשקלים. מה שנשאר הוא להתאים שמות עמודות.

השאר בסקיל — מע"מ, תעודת זהות, טלפונים, הלוח העברי, RLS רב-דיירים — לא נוגע לפרויקט הזה.

---

## ה-API

```
POST   /api/auth/login              כניסה, מחזיר cookie
POST   /api/auth/logout

GET    /api/projects                רשימת הפרויקטים
POST   /api/projects
GET    /api/projects/:slug          פרויקט + מסמכים + קישורים
PATCH  /api/projects/:slug
DELETE /api/projects/:slug

GET    /api/projects/:slug/docs/:docSlug
POST   /api/projects/:slug/docs
PUT    /api/projects/:slug/docs/:docSlug
DELETE /api/projects/:slug/docs/:docSlug

GET    /api/projects/:slug/links
POST   /api/projects/:slug/links
PATCH  /api/projects/:slug/links/:id
DELETE /api/projects/:slug/links/:id

GET    /api/search?q=...            חיפוש חוצה־פרויקטים
GET    /api/backup.zip              גיבוי מלא להורדה
```

### שלוש החלטות שכדאי להסביר

**Cookie ולא JWT ב-localStorage.** טוקן ב-`localStorage` נגיש לכל JavaScript שרץ בדף, כולל סקריפט שהוזרק. Cookie עם `HttpOnly` ו-`SameSite=Lax` לא נגיש ל-JS בכלל. אנחנו כבר מריצים `new Function` ב-runtime ומושכים ספריות מ-CDN — אין סיבה להוסיף לזה טוקן שנשלף בשורת קוד אחת.

**המרת Markdown ל-HTML נשארת בדפדפן.** הפרסר כבר קיים ועובד, וזה גם מה שמאפשר לייב פריוויו בלי לדבר עם השרת בכל הקלדה. השרת מחזיר `content` גולמי, והלקוח מרנדר.

**גבול גודל על `content`.** מסמך Markdown סביר הוא עד כמה מאות קילובייטים. בלי גבול, בקשה אחת עם 50MB של טקסט תתפוס את השרת. המספר: 1MB, ומחזירים 413 מעליו.

---

## העורך

זה החלק שהמשתמש יבלה בו הכי הרבה זמן, אז הוא ראוי לתכנון ולא רק לשורה ברשימה.

### לייב פריוויו

מסך מפוצל: מקור בצד אחד, תוצאה מרונדרת בצד השני. הפרסר כבר קיים ורץ בדפדפן, אז אין כאן קריאת רשת בכלל.

הנקודה שקובעת אם זה ירגיש טוב או גרוע היא **מתי מרנדרים מחדש.** רינדור בכל הקלדה נראה מפתה, אבל על מסמך של אלפי שורות הפרסר רץ מחדש על הכול בכל תו, וההקלדה מתחילה לקרטע. הפתרון הוא debounce קצר — בסביבות 150 מילישניות. מספיק קצר כדי שירגיש מיידי, מספיק ארוך כדי שהקלדה רציפה לא תפעיל אותו בכלל.

שני דברים נוספים שנדרשים כדי שזה יהיה שמיש:

סנכרון גלילה בין החלוניות. כשגוללים במקור, התצוגה זזה למקום המקביל. המימוש הפשוט הוא לפי אחוזים; המימוש הנכון הוא לפי בלוקים, כי בלוק קוד ארוך מזיז את היחס. נתחיל באחוזים.

שמירה אוטומטית, גם היא עם debounce — אבל ארוך יותר, כמה שניות. בלי זה, כל הקלדה היא `PUT` לשרת. עם אינדיקטור "נשמר" שקט, כדי שלא תצטרך לנחש.

הטעות הנפוצה כאן: לחשוב שהפריוויו הוא הבעיה. הסימפטום שמזהים אותה דרכו — העורך מרגיש חלק, אבל השרת מקבל מאתיים בקשות בדקה. הפתרון: שני ה-debounce נפרדים, וזה שמדבר עם השרת ארוך בהרבה.

### לחצני העזר

סרגל מעל תיבת הטקסט שמזריק תבניות Markdown. הקבוצות:

**עיצוב טקסט** — קישור, מודגש, נטוי, קו חוצה, הדגשה (`==`), קוד inline.
**מבנה** — כותרות, רשימה, רשימה ממוספרת, טבלה, קו מפריד, בלוק קוד.
**משימות** — רשימת צ'קבוקסים.
**בלוקים מתקפלים** — `::: details`.
**התראות** — כל הסוגים שהפרסר כבר תומך בהם: `note`, `tip`, `warning`, `danger`, `important`, `info`, `success`, `question`, `example`, `quote`, `experimental`, `deprecated`, `todo`, `abstract`.

הנקודה הטכנית היחידה שמשנה כאן, וקל לפספס אותה: **מה קורה כשיש טקסט מסומן.** אם המשתמש סימן "טקסט חשוב" ולחץ על כפתור ההדגשה, התוצאה צריכה להיות `==טקסט חשוב==` — לא `====` שדורס את מה שסימן. כלומר כל כפתור מוגדר כזוג `before`/`after` שעוטף את הסימון, ולא כמחרוזת אחת שמוחדרת.

אחרי ההזרקה הסמן חוזר לתוך התבנית, לא אחריה. מי שלוחץ על "קישור" רוצה להתחיל להקליד את הטקסט מיד.

הרשימה המלאה של התבניות תשב בקובץ נתונים אחד, לא מפוזרת בקוד — כדי שהוספת סוג התראה חדש תהיה שורה אחת.

### בלוקי קוד בעברית

בלוק קוד מוצג היום `dir="ltr"` תמיד, וזה נכון לקוד. אבל כשמשתמשים בגדר משולשת כדי לשים בתוכה טקסט עברי, התוצאה נשברת: הפיסוק קופץ לצד הלא נכון והשורות מיושרות הפוך.

המימוש מהפרויקט האחר מתועד ב-`docs/Markdown_RTL_Code_Block_GUIDE.md`, ויש בו תנאי אחד שקל מאוד לפספס.

**התנאי הראשון הוא לא כמות העברית — הוא האם הוגדרה שפה.** בלוק שנפתח ב-` ```js ` נשאר LTR גם אם כל ההערות בתוכו בעברית, כי זה קוד. רק בלוק בלי שפה — או עם `plaintext`, `text`, `nohighlight`, `none`, `txt` — הוא בכלל מועמד ל-RTL. בלי התנאי הזה, פונקציית JavaScript עם הערות עבריות הייתה מתהפכת, וזה הרבה יותר גרוע מהבעיה שניסינו לפתור.

רק אחריו נבדק היחס: יותר מ-30% מהתווים האלפביתיים בעברית. הספירה מתוך אותיות עבריות (`U+0590–U+05FF`) ואותיות לטיניות בלבד — ספרות, סימני פיסוק, רווחים וחיצים לא נספרים, אחרת בלוק עם הרבה סוגריים מדלל את היחס ומזייף את התוצאה.

**מה שונה אצלנו, לטובה.** במימוש המקורי הבדיקה רצה על ה-DOM אחרי הרינדור, ולכן היא חייבת לרוץ *לפני* מנוע הצביעה — אחרת המנוע מוסיף `language-*` משלו והבדיקה חושבת שהוגדרה שפה. אצלנו אין את הבעיה הזאת בכלל: הפרסר כבר מחזיק את `b.lang` מהגדר עצמו, בזמן הפרסור, לפני שראינו DOM. הבדיקה הופכת לתנאי על אובייקט הבלוק, בלי `closest('pre')` ובלי תלות בסדר קריאות.

באותו אופן, הקוד המקורי משכפל את ה-CSS בשלושה הקשרי תצוגה (דף MD, דפדפן הריפו, תצוגה מפוצלת). אצלנו יש מסלול רינדור אחד ש-`render_blocks` מייצר, והוא משרת גם את הצפייה וגם את הלייב פריוויו. אותה בדיקה, מקום אחד.

התוצאה בפועל: `dir="rtl"` ו-`textAlign: "right"` על ה-`<pre>`, במקום ה-`ltr` הקבוע שיש היום.

---

## גיבויים

שתי דרכים, ורק אחת מהן מסובכת.

**הורדה לפי דרישה** היא הפשוטה: `GET /api/backup.zip` בונה ZIP בזרימה ומחזיר אותו. בפנים — תיקייה לכל פרויקט, קובץ `.md` לכל מסמך, ו-`links.json` לקישורים. שום דבר לא נשמר בשרת, ולכן אין מה שיתקלקל.

**גיבוי מתוזמן** נכתב לדיסק קבוע ב-Render, שמחובר לשירות ושורד דיפלויים. זו הדרך הפשוטה, בהינתן שהדיסק אפשרי.

שלוש השלכות של חיבור דיסק, שכולן בסדר בפרויקט הזה אבל כדאי שתדע עליהן מראש: השירות מוגבל ל**מופע אחד** ולא יכול להתרחב לרוחב, הדיפלויים מפסיקים להיות zero-downtime כי Render חייב לעצור את המופע הישן לפני שהוא מפעיל את החדש, והדיסק דורש instance בתשלום. לאפליקציית תיעוד של משתמש יחיד — שלושתן לא רלוונטיות.

מה שכן צריך תשומת לב הוא **ניקוי**. גיבוי יומי שנשמר לנצח ימלא את הדיסק, ואז הכתיבה תיכשל — וכמו תמיד, בשקט. שמירה של שלושים גיבויים אחרונים ומחיקת הישנים היא חלק מהמשימה, לא שיפור עתידי.

הדיסק נותן עמידות מול דיפלוי, לא מול אובדן. אם השירות עצמו נמחק, הדיסק הולך איתו. לכן שווה שהגיבוי השבועי יישלח גם החוצה — קובץ לטלגרם מספיק, וכבר יש בוט.

מה שנשמר: הסכימה המלאה, כל המסמכים, הגרסאות והקישורים. מה שלא נשמר: ה-`password_hash`.

---

## אבטחה — מה שקל לפספס

הכללים בקובץ `CLAUDE.md` נוגעים ישירות לכמה מהנקודות כאן, אז אלה לא הערות תאורטיות.

**Race condition ביצירת slug.** אם בודקים "האם ה-slug תפוס?" ואז מכניסים, שתי בקשות במקביל יעברו שתיהן את הבדיקה. הפתרון הוא לא lock בקוד — הוא אילוץ `UNIQUE` בבסיס הנתונים ותפיסת שגיאת ה-constraint. הבדיקה והפעולה חייבות להיות אטומיות, וב-Postgres הן כאלה רק אם ה-DB אוכף אותן.

**פרויקט ציבורי לא חושף יותר ממה שהתכוונו.** נתיב הקריאה הציבורי מחזיר כותרת, תוכן וקישורים. הוא לא מחזיר `owner_id`, לא `updated_at` פנימי, ולא רשימת גרסאות. מודל אחד לתשובה ציבורית, מודל אחר לתשובה מאומתת — לא אותו `to_dict()` עם דגל.

**הודעות שגיאה לא חושפות פנימיות.** `404` על פרויקט פרטי של מישהו אחר ו-`404` על פרויקט שלא קיים — אותה תשובה בדיוק. ההודעה למשתמש בעברית וגנרית; ה-stack trace הולך ללוג בלבד.

**ולידציית מספרים.** `position` מגיע מהלקוח. `NaN` עובר כל בדיקת טווח שכותבים בתמימות, כי כל השוואה עם `NaN` מחזירה `false`. הבדיקה מתחילה ב-`Number.isFinite()`, לא ב-`value < 0`.

**סכמות URL בקישורים.** רשימה לבנה של `http` ו-`https`, כמו שהוסבר למעלה.

**XSS דרך Markdown.** הפרסר בונה אלמנטים של React ולא מזריק HTML — חוץ ממקום אחד, `dangerouslySetInnerHTML` עבור פלט Prism, ושם Prism כבר עושה escape. אבל הלייב פריוויו מרנדר טקסט שנכתב עכשיו, בלי שעבר שום שרת. אם אי פעם נוסיף תמיכה ב-HTML גולמי בתוך Markdown, זה הופך לחור אמיתי — ואז sanitizer, לא regex.

**מחיקה מדורגת.** `ON DELETE CASCADE` על כל המפתחות הזרים, כך שמחיקת פרויקט לא משאירה מסמכים, גרסאות או קישורים יתומים.

---

## שלבים

שבעה שלבים. כל אחד מסתיים במשהו שאפשר להשתמש בו — לא בחצי תשתית.

### 1. תשתית
Postgres ב-Render עם `Asia/Jerusalem` ו-UTF8, שרת, מיגרציות, בדיקת בריאות. `render.yaml` עובר מ-`runtime: static` ל-`runtime: node`, והפרונט מוגש מאותו שירות — כך אין CORS ואין דומיין שני.
**המדד:** `/api/health` מחזיר 200 מ-Render, המיגרציות רצות בדיפלוי.

### 2. כניסה
משתמש יחיד, bcrypt, session ב-cookie, middleware על `/api`.
**המדד:** בקשה בלי cookie מקבלת 401.

### 3. פרויקטים ומסמכים
ה-CRUD המלא, הגרסאות, נתיב הקריאה הציבורי. בלי ממשק — `curl` בלבד.
**המדד:** יצירת פרויקט, כתיבת מסמך, עריכה, וראייה שהגרסה הקודמת נשמרה. פרויקט ציבורי נקרא בלי cookie; פרטי מחזיר 404.

### 4. חיפוש וקישורים
`tsvector` עם משקלים, `strip_nikud`, `pg_trgm` לחיפוש מטושטש. CRUD לקישורים עם ולידציית סכמה.
**המדד:** חיפוש "התקנה" מוצא את המסמך גם אם נכתב מנוקד, וכותרת מנצחת תוכן בדירוג.

### 5. הממשק
זה השלב שנוגע ב-`index.html` הקיים. `localStorage` מוחלף ב-`fetch`, נוסף מסך פרויקטים, הסיידבר מציג את מסמכי הפרויקט הנוכחי, והקישורים בתחתית עמוד הפרויקט. שני העיצובים ממשיכים לעבוד — המסכים החדשים משתמשים באותם משתני CSS.
**המדד:** יצירת פרויקט מהדפדפן, פתיחה ממכשיר אחר וראיית אותו תוכן.

### 6. העורך
מסך מפוצל, לייב פריוויו עם debounce, סרגל הלחצנים, שמירה אוטומטית, וזיהוי RTL בבלוקי קוד.
**המדד:** הקלדה במסמך של אלף שורות לא מקרטעת; לחיצה על כפתור עם טקסט מסומן עוטפת אותו ולא דורסת; בלוק עברי בלי שפה מתיישר לימין, ובלוק ```js עם הערות עבריות נשאר משמאל.

### 7. הגירה וגיבויים
העלאת המסמכים שב-`localStorage` לשרת, `GET /api/backup.zip`, ואחריו הגיבוי המתוזמן לדיסק הקבוע עם מחיקת ישנים.
**המדד:** אחרי ההעלאה, ניקוי נתוני האתר לא מוחק כלום. ה-ZIP נפתח ומכיל את כל המסמכים. אחרי 31 גיבויים, הישן ביותר נמחק.

---

## מה לא נכנס לגרסה הזאת

עריכה משותפת בזמן אמת, הרשאות ברמת המסמך, ייצוא ל-PDF, תגובות, וסנכרון דו-כיווני עם Git.

כל אלה סבירים בהמשך. אף אחד מהם לא נדרש כדי שהמערכת תהיה שימושית.
