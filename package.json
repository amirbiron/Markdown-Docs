{
  "name": "markdown-docs",
  "private": true,
  "description": "בדיקות דפדפן בלבד. האפליקציה עצמה היא Python — ראו requirements.txt",
  "scripts": {
    "check:offline": "node scripts/check-offline.js /tmp"
  },
  "devDependencies": {
    "playwright": "^1.56.1"
  }
}
