[pytest]
asyncio_mode = auto
# לולאה אחת לכל הריצה. המנוע ב-app/db.py נוצר ברמת המודול, ולולאה חדשה
# לכל טסט משאירה חיבורי asyncpg קשורים ללולאה שכבר נסגרה.
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
testpaths = tests
